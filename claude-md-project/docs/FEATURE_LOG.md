# Feature Log

Chronological record of every request made and what was built/changed in response, across the whole conversation that produced this project. Ordered oldest → newest. Use this to understand *why* something exists, or whether an earlier decision was later changed.

1. **Initial build request.** Portfolio site for a designer/artist, minimal/clean/not-colourful style. Built as a single HTML file: hero, work grid, about, contact sections. Dark-neutral palette originally light-toned ("paper"/"ink" tokens named for a light theme).
2. **"Make it interactive."** Added: click-to-filter work grid (initially by medium categories), click-to-expand lightbox with keyboard/arrow navigation.
3. **Clarified the work is 3D and 2D images**, not painting/traditional art. Rewrote all copy, tool tags, and filter categories (Oil/Acrylic/Mixed Media → 3D/2D, then later 3D render/2D illustration) to match.
4. **Uploaded a dark diagonal-line background image**, asked to replace the white background and make it interactive on scroll. Flipped the entire theme to dark (this is when `--paper`/`--ink` values inverted but names were kept). Added scroll+mouse-driven parallax on the background layer.
5. **"Add a particle effect when clicking 3D or 2D."** Built the canvas-based particle burst system, initially only wired to the 3D/2D filter buttons.
6. **Added name/details**: real name (Dineth Meehitiya), bio, contact links (2 emails at the time, ArtStation, YouTube, Instagram), location (Melbourne, VIC), real tool list (Photoshop, Maya, ZBrush, Unity, Unreal Engine, Substance Painter, Blender).
7. **Removed the large hero headline** ("Built in three dimensions, felt in two.") — decided it wasn't needed.
8. **Replaced the background image** with a procedurally generated (Python/PIL) dark ambient image with a soft glow + film grain, specifically to be "simple" and "high quality" without using a stock photo (avoids licensing questions entirely).
9. **Uploaded a custom font (`Retron2000.ttf`)**, asked to replace *all* fonts site-wide. Embedded via `@font-face` base64, applied globally.
10. **Removed the "Work / About / Contact" text nav** from the header (redundant at the time).
11. **Particle effect extended to "All"**, and its colour changed to **dark gold** — but this was done incorrectly at first (applied gold to 3D/Concept/Graphic too). **Corrected**: gold is "All" only, everything else reverted to white/ivory. (See Design System doc — this is a firm rule now.)
12. Shortened the contact heading from "Available for commissions and gallery inquiries." to just "Available for commissions."
13. **Moved the 3 social links (ArtStation/YouTube/Instagram) to sit directly under the "Available for commissions" heading**, separated visually from the email addresses below them.
14. **Added a "Projects" category** (initially empty, no content yet) alongside 3D/2D, with the same particle effect wired up.
15. **Moved bio + portrait image to the top of the page**, removed the (now redundant) old intro paragraph.
16. **Removed two bio paragraphs** ("My practice moves between mediums..." and "Based in Melbourne, VIC. I take on a small number...") leaving just the single lead paragraph.
17. **Added top-right icon links**: YouTube, ArtStation, Instagram as inline SVG icons in the header, replacing/alongside the text nav removed earlier.
18. Person reported the icon links "aren't working" — investigated; HTML/hrefs confirmed correct. Likely cause: sandboxed in-chat file preview blocking `target="_blank"` navigation, not a real bug. **Not independently re-confirmed working** by the person after being told to test in a real browser tab — worth re-checking if raised again.
19. **Added LinkedIn** — both as a 4th top-right icon and as a text link in the contact section.
20. Removed "— Est. 2016" from the "3D & 2D Artist" label.
21. **Added a full 3D sub-filter system**: clicking "3D" reveals a second row (All 3D / 3D Modelling / 3D Printing / 3D Animation). Retagged existing pieces into these sub-categories, added one placeholder piece ("Cast Form") to fill the otherwise-empty Printing sub-category.
22. **Replaced "Concept Art" and "Graphic Design" as top-level filters with a single "2D" top-level filter**, given the same sub-filter treatment as 3D (All 2D / Concept Art / Character Art / Graphic Design — Character Art added as new). Retagged existing pieces accordingly (one piece relabelled from Concept Art to Character Art to populate the new sub-category).
23. **Uploaded 3 real poster pieces** (Wings & Waves, Spectral Emergence, Monarch — Butterfly Effect) and added them as real Graphic Design pieces, replacing the two placeholder pieces that existed before.
24. **Removed the grayscale-by-default / colour-on-hover effect** on all images — everything shows full colour all the time now.
25. **Uploaded 2 framed mockup images for Wings & Waves**, asked to add them as additional views *without changing the front/default image*. Built the gallery-thumbnail pattern (see Design System doc) to support this — this was the first use of the `data-gallery`/`gallery` field.
26. **Repeated the same gallery pattern for Monarch — Butterfly Effect** (2 mockups) and **Spectral Emergence** (2 mockups).
27. **Uploaded 3 new 3D render images (Keys)**, added as a brand-new 3D Modelling piece (not a gallery addition to an existing one) — first image as the front/main, other two as gallery extras.
28. **Repeated for "Treasure Chest"** (4 images: 1 front + 3 gallery), **"Shotgun"** (4 images: 1 front + 3 gallery), **"Nuka Cola"** (4 images: 1 front + 3 gallery), **"TNT"** (3 images: 1 front + 2 gallery) — all added as new 3D Modelling pieces following the same established pattern.
29. Person asked again whether the top-right links work — re-explained the likely sandboxed-preview cause, no code change made (nothing found to fix).
30. **CV/resume request.** Clarified requirements first (open in new tab; place in contact section). Person uploaded their real CV PDF. Embedded it as a base64 `data:application/pdf` link, added a "CV" text link in the contact section. Flagged that the CV contains personal info (phone number, two references with their own contact details) — person did not ask for redaction.
31. **Added a CV icon to the top-right icon row too** (document-shaped SVG), linking to the same embedded PDF.
32. **Removed the gmail address** from contact (kept only the hotmail address). **Added two new Projects sub-categories: "Animation" and "Game"** — built as a second sub-filter row under the "Projects" top-level filter, same pattern as 3D/2D.
33. **Added a real YouTube video** (as a playable embedded piece, not just a thumbnail/link) under Projects → Game. Built the video-piece pattern: YouTube-hosted thumbnail with CSS play-button overlay in the grid, real `<iframe>` embed in the lightbox/detail view, playback stopped on close.
34. **Repeated the video pattern for Projects → Animation** with a second real YouTube video.
35. Person asked about pricing (a "$30 package") to "do better" — researched Anthropic's current plan pricing to answer accurately, but the person then said to set pricing aside and focus on the build. No code changes.
36. **The big rebuild request.** Person shared two wireframe images and a detailed explanation of a much more ambitious vision: a central rotating 3D "icon in a glass/reflective orb" as the homepage centerpiece, doubling as a toggle to a separate "About Me" page; three category labels (3D Art / 2D Art / Projects) branching into sub-category lists on hover; clicking a category/sub-category leads to its own page of work (reusing the particle effect); clicking a work item leads to a dedicated page explaining it; full back-navigation throughout; the existing "Available for commissions" section reachable by scrolling down on the home page. Explicitly asked to **keep the existing colour palette, typography, and particle effect** (experimentation with the effect allowed but optional), and to use placeholder images where a real "icon" asset doesn't exist yet.
    - Clarified two open decisions before building: **(a)** orb fidelity — chose **true 3D WebGL glass sphere (Three.js)** over a cheaper CSS/SVG stylized approximation; **(b)** Projects sub-items — chose **both** a year filter *and* keeping the existing Animation/Game type tags (not replacing one with the other).
    - Built **`portfolio_v2.html`**: extracted all 15 real work pieces (with their full images, galleries, and video IDs) out of `portfolio.html` into a single `WORK_DATA` JS array so nothing had to be re-uploaded; reused the header, contact section, about content, fonts, background, and particle system verbatim; built a new four-screen "fake SPA" (Home / About / Category / Detail) with a Three.js glass-sphere orb (procedural environment map, auto-rotation, mouse-tilt, click/keyboard-activatable, navigates to About) and hover-expanding category labels on the home screen. Category and Detail screens are fully dynamic, rendered from `WORK_DATA` at runtime rather than hand-written per piece. Projects category got the dual year/type-tag filtering as agreed.
    - Flagged as first-pass/known-rough: the orb's visual quality (lighting/environment/icon integration), lack of real per-piece descriptions on detail pages, placeholder titles on two pieces, no mobile-specific tuning yet, no browser-history integration.
37. **This documentation request** — asked for a `CLAUDE.md` and supporting docs capturing all of the above so work can continue in a fresh Claude Code repo. Produced this file, `CLAUDE.md`, `docs/DESIGN_SYSTEM.md`, and `data/work-data.json` (the real content data extracted to a standalone, reusable file).
38. **Looping VFX background + orb polish.** Person supplied a video clip (`Background vfx/45947-447087546_medium.mp4`) and asked for it to loop as the site background, for the glass orb to show a reflection, for text to stay legible over the busier moving background (blur allowed), and for the orb's icon to be mouse-draggable.
    - Replaced the static `#bg-layer` `<div>` (CSS `background-image`) with a `<video autoplay muted loop playsinline>` embedded as a base64 `data:video/mp4` source, following the existing single-file-embed convention. The old procedurally-generated ambient JPEG is kept as the video's `poster` (shown before the first frame decodes) instead of being discarded.
    - Added `blur(2.5px)` to the background's CSS filter (on top of the existing scroll-driven brightness/saturate) and darkened the `#bg-overlay` gradient stops, specifically so body text stays readable over the moving footage.
    - The orb's glass material (`scene.environment`) now uses a `THREE.VideoTexture` reading live from the same `#bg-layer` video element (equirectangular-mapped) instead of only the hand-drawn gradient canvas, so the glass reflects the actual moving background. The gradient canvas texture is kept as the fallback when `prefers-reduced-motion` is set (video background is also paused in that case, consistent with the existing reduced-motion convention).
    - Icon inside the orb is now draggable: `pointerdown`/`pointermove`/`pointerup` on the orb canvas reposition the icon plane's x/y (clamped so it stays inside the glass), while a genuine click (no drag movement) still navigates to the About screen as before — distinguished via a movement-threshold flag so drag and click-to-navigate don't conflict.
    - Known tradeoff: embedding the ~5.6MB video as base64 grows `portfolio_v2.html` from ~6MB to ~13.6MB. Kept consistent with the project's existing "single self-contained file" convention rather than linking it externally; revisit if the person wants a leaner file (see Open Questions in `CLAUDE.md`).
39. **Intro/cover splash screen + header logo.** Person uploaded a new `Logo/` folder with two brand-mark PNGs (`cover logo background DM.png`, 1920×1080, and `Logo DM.png`, 891×1080) and asked for the first to show as a full-screen cover *before* the existing home screen, dismissing with a "zoom into the main page" click, and the second to sit next to the name in the header.
    - Added `#cover-screen`, a new fixed full-viewport layer (`z-index:300`, above everything including the header) rendered as the very first element in `<body>`, showing `cover logo background DM.png` as its background image plus a "Click to enter" hint. Clicking (or `Enter`/`Space`) triggers a `cover-zoom` class (CSS `transform:scale(2.6)` + `opacity:0` over 0.85s, mimicking zooming through the mark into the site) then, once the transition ends, a `cover-hidden` class (`display:none`) removes it from layout and the tab order. Skips the scale animation (just a quick fade) under `prefers-reduced-motion`, matching the rest of the site's convention.
    - **Notable asset quirk:** `cover logo background DM.png` is not a flat black-background-with-white-logo image — pixel-inspection showed it's an opaque black RGBA field with the "DM in circle" mark cut out as fully *transparent* pixels (RGB happens to be 0,0,0 under the transparency too, which is why it silently rendered as a plain black rectangle at first). Fixed by giving `#cover-screen` a `background-color:var(--ink)` sitting *underneath* the background-image, so the transparent cutout reveals the off-white mark against the opaque black field. Worth knowing if more assets come from the same export pipeline — check for this pattern before assuming an image will "just look right."
    - Added `Logo DM.png` (a normal white-mark-on-transparent PNG, no quirks) as a small `<img class="nav-logo">` inline right after the "Dineth Meehitiya" text in the header's `.nav-name`, 24px tall.
    - Both images embedded as base64 (49KB and 78KB raw, small enough not to meaningfully affect file size), consistent with the single-file convention.
40. **Orb carousel rebuild.** Person shared a reference screenshot (three wireframe-textured spheres in a row — "2d Art" / larger centred "about me" / "3d Art" — with circular swipe-arrows either side) plus a new `Orbs/` folder containing a licensed stock texture (`18566403695347.jpg`/`.eps`, "ultra thin line" distorted-sphere photo) and asked to: (a) apply that texture to the orb so the icon stays visible "around it"; (b) change the centre icon from a circular crop to a plain rectangle that only hovers left/right, not all directions; (c) turn the single orb + text-label-column home layout into a 3-orb carousel — centre orb fixed as "about me", side orbs swipeable through 3D/2D/Projects — where clicking a side orb goes straight to that category's "All" view and hovering it reveals its sub-categories.
    - **Flagged the texture file's licensing before touching it**: its EXIF description read like an Adobe Stock listing and "ULTRA THIN LINE" was baked into the image. Person confirmed they hold a license, so proceeded using it as supplied.
    - **Clarified the carousel's interaction model before building** (screenshot alone didn't specify it): confirmed About Me stays permanently centred with the arrows only cycling which 2 of the 3 categories sit beside it (not a 4-way loop), and that clicking a side orb navigates straight into that category rather than requiring a "swipe to centre, click again" two-step.
    - **Texture prep:** no Python/ImageMagick available in this environment, so used PowerShell's built-in .NET `System.Drawing` to centre-crop the 6000×3375 source down to a clean 900×900 square (cropping out the "ULTRA THIN LINE" caption baked into the bottom of the original) → `Orbs/orb-texture-900.jpg` (~230KB), which is what's actually embedded.
    - **Scene rebuild** (`portfolio_v2.html`, the `initOrb` function): one canvas/camera now renders three orb groups — `centerGroup` (x=0, radius 1.7, contains the icon) and `leftGroup`/`rightGroup` (x=±2.6, radius 1.05, no icon). Every orb = the existing glass `MeshPhysicalMaterial` sphere + a new same-radius flat `CircleGeometry` disc using the line texture as `map` with `THREE.AdditiveBlending`+`depthWrite:false`, so the bright lines glow on top of the glass/icon without an alpha-masking step (black in the source photo contributes nothing when added). `scene.environment` (the reflective bg-video texture) is shared across all three automatically since it's a scene-level property.
    - **Icon**: swapped `CircleGeometry(1.05,48)` → `PlaneGeometry(1.3,1.62)` (a rectangle). Removed the `iconMesh.rotation.x` line entirely so only Y-axis (left/right) tilt applies; the drag handler was similarly cut down to X-only.
    - **Carousel state**: `CAROUSEL_ORDER = ['2d','3d','projects']` with a single rotating `carouselLeftIdx` — the arrows just increment/decrement it mod 3 and re-render the left/right slot labels, hover-flyouts, and click targets. The 3D meshes themselves never move or change when swiping; only the DOM labels swap (with a quick 150ms fade), which was a deliberate simplification over animating mesh positions/textures per category.
    - **DOM/3D alignment**: the side-orb hotspots, labels, and hover-flyouts are plain DOM elements (a `<button class="orb-hotspot">` per side, not raycasting) positioned via a `left%` computed from the *same* camera-projection formula used to place the actual 3D meshes (`halfWidth = camera.position.z * tan(fov/2) * camera.aspect`), recalculated in `resize()` every time the canvas size changes — verified against `THREE.Vector3.project()` to confirm the DOM overlay and the rendered mesh agree to sub-pixel accuracy. The same projection is used to gate the centre orb's existing click/drag/tilt handlers to a centre hit-region, since the canvas is now wide enough to also cover the two side orbs.
    - **Known scope cut**: side orbs auto-rotate slowly but don't tilt toward the cursor the way the centre orb does — flagged in `CLAUDE.md` as a deliberate simplification, not a bug, given how much this one request already touched.
    - File grew from ~13.7MB to ~14.1MB (the 900×900 texture, embedded once and reused by all three orbs).
41. **Orb diagram v2 — diamond layout, drag-to-spin, wireframe material.** Person came back with an updated reference mockup: About Me on top (larger), 2D/3D/Projects arranged below it (no swipe arrows visible), and reported the previous line-texture orbs were **"not visible at all"** in practice. Asked for: the diamond layout; each orb individually draggable/spinnable left-right; hover-to-reveal-subcategories kept; placeholder photos inside every orb (not just About Me); and "a simple colour texture or material... wireframes or Y2k textures would be cool" to actually make the orbs visible.
    - **Replaced the swipe carousel with a fixed 4-orb diamond**: `ORB_DEFS` now lists all four orbs at once (about: top/large; 2d: left; 3d: right; projects: bottom-centre) with explicit world x/y/radius — no more `CAROUSEL_ORDER`/`carouselLeftIdx`/arrow-swap state, since there's room to show all three categories simultaneously. Camera moved from `z=6` to `z=9.5` (same 38° fov) so the taller vertical spread fits the frustum without clipping the top of the About orb or the bottom of Projects.
    - **Dropped the line-texture material entirely** — it used `THREE.AdditiveBlending` against a mostly-black source photo, which apparently rendered as good as invisible in the person's actual browser even though it tested fine here. Replaced with a `THREE.SphereGeometry(radius*1.04, 28, 20)` in `wireframe:true` mode, colour-swept per-vertex (`geometry.setAttribute('color', ...)` + `vertexColors:true` on a `MeshBasicMaterial`) through a soft blue→violet→pink HSL sweep at 45% saturation — reads as "Y2k holographic chrome" without breaking the site's established "not too colourful" rule, and — critically — is genuine WebGL wireframe geometry, so it can't silently fail to render the way a texture/blend-mode combination can.
    - **Every orb now has an icon**, not just About Me — reused the placeholder-photo pattern (`PlaneGeometry`, sized proportionally to each orb's radius) with a different picsum.photos ID per orb for visual variety ("any is fine" per the person). All are still placeholders; flagged in `CLAUDE.md` to swap for real images later.
    - **New interaction model — drag-to-spin, not tilt-to-cursor**: every orb (not just the centre one before) now has its own dedicated DOM `<button class="orb-hotspot">`, sized/positioned every `resize()` via the same camera-projection math as the mesh. Dragging left/right on any orb accumulates into a per-orb `rotY` (`pointermove` delta × 0.012) that drives that orb's glass sphere + wireframe + icon rotation together — replacing the old passive "tilts toward wherever the cursor is" behaviour with an explicit grab-and-spin interaction, per the person's request ("moving around with the orb, rotating them left or right"). A movement-threshold flag (same pattern as every other drag in this project) distinguishes a spin from a plain click so click-to-navigate still works. All orbs also keep a slow randomised auto-rotation when idle.
    - Because every orb now owns its own hotspot, the old "gate canvas-wide events to a centre hit-region" logic (needed when one wide canvas covered 3 orbs in a row) was deleted outright — `#orb-canvas` is now `pointer-events:none` and purely a rendering surface; the browser's native hit-testing on the 4 buttons handles everything, including keyboard access (real `<button>`s get Enter/Space activation for free, no custom keydown code needed anymore).
    - File size dropped back from ~14.1MB to ~13.7MB now that the (non-working) 900×900 texture is no longer embedded.
42. **Orb diagram v3 — swipeable row, brighter/dimmer wireframe iteration, floating, layout polish.** Person shared an annotated version of the v2 diamond screenshot with arrows pointing the three category orbs further down, plus a batch of asks: reposition the 3 category orbs lower (between About Me and the fold); make the whole diagram swipeable left/right to choose which orb is "selected in the middle"; make About Me bigger; move the category label text underneath the orbs and make it ALL CAPS; move the scroll-cue lower if needed and delete the hairline border above it entirely; make the wireframe white and dimmer; make the orbs float up/down slowly.
    - **Asked a clarifying question before touching the architecture again**: "swipeable... choose which orb in the middle" had at least two readings (cycle the 3 category orbs through a centre slot, vs. swap out About Me itself for a different big centre orb). Person confirmed: the 3 category orbs sit in a row below a *fixed* About Me, and swiping cycles which one occupies the centre position.
    - **Replaced the diamond with: fixed About Me (top, enlarged `radius 1.5→1.85`) + a swipeable 3-orb row** lower down (`y:-2.05`, vs. the diamond's `-0.4`/`-1.95` — direct response to "positioned lower, between the middle orb and the bottom section"). Camera pulled back again, `z=9.5→10`, to keep the taller layout inside the frustum.
    - **Row reassignment, not remeshing**: the three row *positions* (`rowLeft`/`rowMid`/`rowRight`, fixed world x −2.1/0/+2.1) are permanent meshes; a `rowOrder` array (e.g. `['2d','3d','projects']`) says which category's texture/label/flyout/click-target is currently bound to each position. `applyRowOrder()` re-points all of that; the meshes themselves never move, matching the same "relabel, don't remesh" approach used for the original (now-removed) carousel arrows. Swiping is a drag gesture on the diagram background (anywhere that isn't directly on an `.orb-hotspot`, checked at `pointerdown` so it never fights with the existing per-orb spin-drag) past a ~40px threshold, rotating `rowOrder` by one with a quick 150ms label fade.
    - **Wireframe iterated again**: the previous "Y2k holographic" per-vertex rainbow sweep is gone — plain white (`0xF2F0EA`), `opacity` dropped from `0.75` to `0.3`, no `vertexColors`. Simpler and, per the person, easier to actually see against the dark background than the coloured version was.
    - **Floating**: each orb's `THREE.Group` now oscillates its own `position.y` around its base position (`baseY + sin(t·floatSpeed + floatPhase) × 0.09`), with `floatSpeed`/`floatPhase` randomised per orb at build time so all four don't bob in lockstep. Runs alongside the existing per-orb spin (`rotY`) in the same `animate()` loop; skipped under `prefers-reduced-motion` like all other motion on the page. Note: the DOM hotspot/label positions are computed from each orb's *base* x/y (not the live floating offset), so there's a small, deliberately-accepted mismatch between the hotspot circle and the visually-bobbing sphere — not worth the cost of repositioning DOM every frame for a few pixels of drift.
    - **Labels**: `.orb-slot-label` switched from `text-transform:lowercase` to `uppercase` (CSS only — the underlying JS strings didn't need to change). Confirmed already rendering below each orb in the DOM/flow; the earlier diamond's cramped vertical space was the more likely culprit for anything reading as "not underneath," which the lower-row change and taller `.orb-diagram` height also address.
    - **Removed the hairline border** between the hero and the "Available for commissions" section (`.contact{border-top:1px solid var(--line)}` → deleted) and added top margin/padding to `.scroll-cue` so it has clear space below the now-lower row of orbs.
    - Verified via direct interaction (not just code review): swiped the row and confirmed both the DOM labels *and* the 3D icon textures update together; clicked a freshly-swiped-in orb and confirmed `showCategory` receives the *new* category, not a stale one; confirmed floating + rotation are both live by diffing two screenshots ~2s apart.
43. **Orb diagram v4 — real animated 3D carousel.** Person sent an annotated reference image (large centre sphere, two smaller spheres pulled in behind it with circular swap arrows) and asked specifically for the 3-orb row to become a proper animated carousel: centre orb always selected/largest, sides smaller and pushed back for depth, swipe left/right smoothly rotates which orb is centred (large↔small scale and position/depth changes all animating, not popping), clicking a side orb animates it into the centre. Explicitly asked to inspect the current implementation first and reuse its structure rather than building a parallel system, and not to touch anything outside the 3-orb row.
    - **The previous "row" (added last iteration) only *looked* like a carousel** — it was actually 3 fixed mesh positions that instantly swapped which category's label/texture/click-target was bound to them (a deliberate simplification at the time, flagged as such in `CLAUDE.md`). That's what had to change: this request specifically wants the *meshes* to move, with real position/scale/depth easing.
    - **Restructured around persistent per-category meshes instead of persistent per-position meshes**: `categoryOrbs['2d'|'3d'|'projects']` are each built once and never destroyed; `CAROUSEL_SLOTS.left/center/right` are named target transforms (`{x, z, scale}` — centre `{0, 0.3, 1.25}`, sides `{±1.6, -1.1, 0.55}`); `slotAssignment` is the only thing that changes on a swipe/click (which category → which slot name). Every frame, each category orb's `curX`/`curZ`/`curScale` eases 9%/frame toward whichever slot it's currently assigned — so re-assigning a slot makes that orb *visibly glide* to its new position/size/depth over ~0.5s, rather than jumping.
    - **The "goes around behind, not through" carousel feel came free from the math**, not from any special-casing: because the left and right slots share the same `z` (both pushed back) while only the centre slot sits at a different `z`, an orb animating from left-slot to right-slot (or vice versa) has its z stay roughly constant throughout — so it visually arcs *behind* whatever's currently in the centre rather than swelling through the front of it. Independently-eased x/y/z/scale was enough; no bezier paths or angle-based parametrization needed.
    - **Click behaviour changed for side orbs on purpose**: previously clicking *any* row orb (left/right/centre) navigated straight to that category. Now only clicking the **centre** slot navigates; clicking a **side** slot calls the exact same `rotateCarousel()` the swipe gesture uses, spinning that category into the centre without navigating — i.e. it now takes "select, then confirm" (2 clicks) to enter a category you didn't start centred on, matching the carousel-picker behaviour the person described ("clicking...should automatically animate that orb into the centre and make it the selected orb," with no mention of navigating). Confirmed this doesn't regress the centre-click path — clicking the centred orb still calls `showCategory()` immediately, same as before.
    - **DOM hotspots stay position-based** (`rowLeft`/`rowMid`/`rowRight`, unchanged ids/screen locations from before) but their *size* and *content* (label, `aria-label`, sub-flyout) now update instantly to the destination slot the moment a swap starts (`updateSlotDom()`), rather than animating in step with the mesh — a deliberately accepted small mismatch during the ~0.5s transition (hotspot already "final," sphere still catching up) rather than the added complexity of animating hit-targets every frame.
    - Per-orb drag-to-spin (Y-axis, unchanged from prior iterations) now looks up which category currently owns a given slot *live* (`categoryOrbs[slotAssignment[slotName]]`) rather than being bound to a fixed orb reference, so spinning still targets the right mesh even mid-carousel-animation.
    - Verified via direct interaction: swiped repeatedly and confirmed label/texture/size all move together in each screenshot; clicked a side orb and confirmed via DOM query it animates into centre *without* navigating (`screen-category` stays inactive); clicked the (now-centred) orb and confirmed it navigates with the correct, freshly-selected category title. Also confirmed the "seed" preview tab's screenshots and one-off DOM reads can disagree with each other mid-session (a known quirk of this environment, not the site) — cross-checked state via direct DOM queries rather than trusting a single screenshot when the two disagreed.
44. **Orb diagram v5 — spacing fix, scroll-cue clipping fix.** Person sent screenshots (from an actual Chrome tab, not this session's preview pane) showing the row's centre orb visually overlapping/touching About Me, and the "Scroll down for commissions & contact" hint getting sliced by the bottom of the browser window. Asked for clear horizontal spread between the 3 row orbs, more vertical gap below About Me, and the scroll-cue to be fully visible with intentional spacing — explicitly **not** by just pushing the whole section down (wanted the composition to stay visually centred/balanced), and explicitly keeping About Me's own position/size untouched.
    - **Diagnosed the overlap with the actual numbers, not just eyeballing**: at the old constants (`ROW_Y:-2.05`, centre slot `{x:0,scale:1.25}`, About `{y:1.2,radius:1.85}`), About's bottom edge and the row's top edge were only ~0.03 world units apart *before* accounting for the floating idle animation (±0.09 each) — meaning they were guaranteed to overlap on every float cycle. Horizontally, the old side slots (`x:±1.6`) were closer to the centre than the sum of the centre and side orb radii (1.6 < 1.248+0.572), so the side orbs overlapped the centre one too, not just About Me.
    - **The constraint that made this non-trivial**: About Me's position/radius were declared off-limits, but the vertical room available *below* About Me within the existing camera frustum (`halfHeight`, fixed by `camera.z` and `fov`) was nearly used up already — simply pushing `ROW_Y` further down without more room would clip the row against the bottom of the frustum instead.
    - **Fix: scale the camera back and About Me together, by the same factor, so About Me's *on-screen* result is unchanged but there's more absolute world-space to spread the row into.** `camera.position.z: 10→12.5` (×1.25) paired with `ABOUT_DEF.y: 1.2→1.5` and `ABOUT_DEF.radius: 1.85→2.3125` (both ×1.25) — since screen position/size for a fixed-FOV perspective camera depend only on the *ratio* of an object's y/radius to the frustum's half-height (which scales by the same 1.25× from the camera move), the ratio — and therefore every pixel of About Me's rendered position and size — is unchanged. This freed up real room to work with: `ROW_Y: -2.05→-2.75` (more gap below About) and `CAROUSEL_SLOTS` left/right `x: ±1.6→±2.6` (real separation from the centre orb and from each other), with a small tweak to centre `scale: 1.25→1.2` to keep a comfortable margin against the frustum's bottom edge even at the float animation's lowest point.
    - **Scroll-cue clipping wasn't an `overflow:hidden` bug** — checked, there wasn't one (`body{overflow-x:hidden}` is horizontal only). It was `.home-hero{min-height:calc(100vh - 81px)}` combined with `.scroll-cue` being a *sibling* positioned immediately after the hero closed: the hero reserved exactly the initial viewport, so the cue's box started precisely at the fold and got window-edge-sliced on load. Fixed by moving `.scroll-cue` **inside** `.home-hero` as a second flex child and setting `.home-hero{flex-direction:column}`, so the diagram-plus-cue now centre as one block (`justify-content:center` on the column) — the cue always has its own margin above and below rather than landing exactly on the viewport boundary. This also happens to be why the fix doesn't "just push the section down": nothing about `.home-hero`'s own height/position changed, only what's inside it and how that content is arranged.
    - Verified in-browser: re-screenshotted the home screen and confirmed a clear visible gap between About Me and the row with no overlapping wireframes anywhere (previously touching/merging), scrolled down and confirmed the full scroll-cue text renders with clean spacing above and below it, and re-ran the click-to-centre interaction test (click a side orb → confirm via DOM query it's now the centre slot's occupant, without navigating) to confirm the position/scale changes didn't regress hit-testing on the relocated hotspots.
45. **Orb glow + ambient dust.** After confirming the download/preview confusion from #44 was actually Microsoft Teams' sandboxed file viewer (not a real bug — flagged as the likely explanation, matching the identical "icon links not working" pattern from way earlier, now effectively confirmed), person came back once they had it open in an actual Chrome tab and asked to make the orbs "look more like this," sharing a moodboard-style reference image (soft glowing wireframe spheres with faint drifting dust/stars around them) with an explicit "still keep how the background is also" — i.e. enhance the orbs, don't touch `#bg-layer`.
    - **Treated the reference image as a style/vibe reference, not a literal spec** — it had tells of being AI-generated (a visible "Grok" watermark, and a duplicated "3D ART" label that reads as a generation artifact rather than an intentional design element), so the goal was "glowing orbs + ambient dust," not pixel-matching every detail (skipped the reference's scanline/VHS grain texture over the whole image, which would have been a bigger departure from the site's established minimal look than asked for).
    - **Glow implemented as an additive-blended `THREE.Sprite`** (`makeGlowSprite`), not a full post-processing bloom pass — deliberately avoided pulling in Three.js's `EffectComposer`/`UnrealBloomPass`/etc. (more CDN script tags, more complexity) when a soft radial-gradient sprite behind each orb, always facing the camera automatically (that's what sprites do), gets a very similar visual result far more cheaply. The gradient texture is a small procedural `CanvasTexture` (same technique already used elsewhere in this file for the env-map gradient), generated once and shared by every orb's glow sprite and by the dust field below.
    - **Glow sprites are children of each orb's existing `THREE.Group`**, so they automatically inherit that orb's live position and scale for free — no extra per-frame code needed even for the carousel orbs, whose `group.scale` is already being eased every frame for the carousel effect.
    - **Ambient dust** is a single scene-wide `THREE.Points` cloud (~160 specks, reusing the same glow texture, additive blending) spread through roughly the visible volume, given a very slow independent rotation in the existing `animate()` loop (gated behind `!reduceMotion` like everything else). It's one extra object, not per-orb, since it's meant to read as ambient atmosphere behind the whole diagram rather than something attached to any single orb.
    - Verified in-browser (fresh load, `window.onerror` check — no exceptions) that every orb now shows a visible soft halo and small dust specks are visible scattered through the scene, and re-confirmed a click still navigates correctly (About Me hotspot → `#screen-about`) to make sure the purely-additive scene objects didn't regress hit-testing or interaction.
46. **Header bar removed; icon "tearing" fixed.** Person sent two annotated screenshots (red arrows/handwriting) from a real Chrome tab: one pointing at the thin line under the header ("the bar"), and a second showing a visible diagonal seam/ghosting splitting the icon photo inside the "3D Art" orb, plus a follow-up mid-turn message ("also there on in the bottom where the cut is happening") pointing at the same tearing on another orb — confirming it wasn't isolated to one orb or one camera angle.
    - **The bar**: `header{border-bottom:1px solid var(--line)}` → removed the property entirely. One-line fix.
    - **The "cut"/tearing was a real, structural rendering bug, not a screenshot-timing artifact** — worth noting because earlier in this project, similar-looking glitches in *my own* screenshots were reasoned away as "mid-rotation-frame WebGL timing," which was true for the "thin vertical sliver" look (that's just the icon plane viewed edge-on, a normal consequence of continuous auto-rotation) but was the wrong explanation for *this* symptom. This one was a real bug in every orb, visible at some rotation angles more than others, that the person's screenshots from an actual browser caught clearly.
    - **Root cause**: the glass material used `transmission:1.0` for true physically-based refraction. Three.js implements `transmission` by rendering the scene into a background texture (excluding only the transmissive object itself) *before* the main pass, then sampling that texture with IOR-based distortion when drawing the glass. Because the icon `PlaneGeometry` sits *inside* the glass sphere's bounds, it can end up captured into that background texture *and* rendered normally on top in the same frame — a duplicate, refraction-distorted copy of the icon showing through alongside the crisp real one, which reads as a torn/offset seam wherever the two don't line up.
    - **Fix**: `makeGlassMat()` changed from `transmission:1.0` to `transmission:0` with `opacity:0.16` (plain alpha blending) — this skips the background-capture pass entirely, so there's nothing for the icon to get double-rendered into. `clearcoat`, `clearcoatRoughness`, `roughness`, `ior`, and `envMapIntensity` were all left as-is, since clearcoat reflections (a separate physical layer from transmission) still pick up the background-video environment map fine without it — the orb keeps its reflective sheen, it just lost the true see-through refraction, which was never the visually load-bearing part of the "glass" look anyway (the wireframe + glow carry that).
    - Verified in-browser across a full rotation cycle (not just one frame): waited for different orbs to rotate to a front-facing angle and confirmed each showed a single, clean, undistorted icon with no seam — previously *every* orb showed the tear at some point in its rotation, now none do. Also reconfirmed no console errors on a fresh load and that the header's sticky/blur behaviour is otherwise unchanged.
47. **Big interaction-flow rebuild, kicked off with Step 01 (DM logo → scatter → enter).** Person shared an annotated 5-step sketch sheet plus reference images (mouse-trail dots, a Pinterest "category panel" layout, a Pinterest letter/number-trail background style, a typography/box treatment) and a separate clean "DM" logo mark (bold wordmark plus decorative outline squares/triangles/pill shapes), along with a full written breakdown translating the sketch into a 10-step build plan (scatter-intro → orbs fly into position → hover zoom+info box → click-to-split into categories → category panel UI → half-orb click to reassemble → mouse trail → typography pass → polish/responsive). Explicitly asked to build this **in stages**, one step at a time, and to **modify the existing orb/particle/background systems rather than rebuild from scratch**. Only Step 01 was implemented in this pass.
    - **Step 01 scope**: replace the old `#cover-screen` (a flat PNG with a transparent DM cutout, background-color hack to reveal it) with a click-to-scatter intro — decorative shapes fly outward first, *then* the existing zoom/fade transition (`cover-zoom`) plays and the screen unmounts into `#screen-home`, reading as the page "unfolding" rather than a plain fade.
    - **The pasted logo image couldn't be embedded directly** — pasted chat images in this environment aren't written to disk anywhere reachable by file tools, only the base64-PNG-background approach the old cover screen already used would have worked, and no such file was available this time. Rebuilt the mark as **inline SVG** instead (2 pill/capsule outlines, 2 nested-square pairs, 2 overlapping-triangle pairs, and a `<text>` "DM" wordmark in the site's existing Retron2000 font) — a deliberate, necessary tradeoff, not just a stand-in: vector shapes as separate SVG elements can each be animated outward independently on click, which a flat raster image cannot do. Traced loosely from the reference image's composition/proportions, not pixel-exact (skipped the reference's stepped connector line between shapes as the least essential piece). If the person wants the literal pasted mark instead, they'd need to drop the actual file into `Logo/` for a follow-up pass.
    - **Two-phase transition, not one**: click → `.cover-mark` gets a `.scattering` class (each `.cover-shape` has its own `--dx/--dy/--dr` custom properties driving an outward translate+rotate+fade over 0.5s, styled via a shared `.cover-mark.scattering .cover-shape` rule so no per-shape JS is needed) → after a fixed 520ms `setTimeout` (not `transitionend`), the existing `cover-zoom`/`cover-hidden` zoom-fade-out logic runs unchanged. Deliberately **not** wired through `transitionend` bubbling for the shape phase: `transitionend` bubbles up from descendants, and the original code already listens for one `transitionend` on `#cover-screen` itself (`{once:true}`) to trigger `finish()` — if the shape-scatter transitions were left running while that listener was attached, the *first* shape to finish scattering would fire `finish()` early and hide the screen mid-zoom. Splitting into two explicit sequential phases (shapes finish, only then does the zoom-phase listener get attached) avoids that collision entirely.
    - **`prefers-reduced-motion` handling**: unchanged behavior preserved exactly — if `reduceMotion` is true, `enterSite()` skips the scatter phase entirely and calls the zoom-out step directly, same as the site-wide blanket `*{transition:none!important}` rule already guaranteed for the old flow.
    - **`#cover-screen` background simplified**: since the mark is now vector shapes drawn in `var(--ink)`, the div's background is now a plain `var(--paper)` fill — removed the old fragile "opaque-PNG-with-transparent-cutout over an `--ink` background-color" setup (and the ~48KB of now-unused base64 PNG data that went with it) called out in CLAUDE.md as something to watch if the asset ever changed. That fragility is now moot.
    - **Editing mechanic worth remembering**: this file has a few individual lines that are megabytes long (embedded base64 media), so line-number-based `awk` splicing has to re-read the *current* line number right before each edit, not reuse one captured earlier in the same session — a prior CSS/JS edit in this same pass shifted the target line down by ~8 rows and an initial attempt spliced the new markup into the wrong line (clobbering a `prefers-reduced-motion` CSS rule) before being caught and corrected by re-scanning line lengths.
    - Verified via direct DOM/JS execution in the preview tab (not screenshots, since the Browser pane wasn't displayed client-side for this session): confirmed the SVG mount (10 `.cover-shape` elements, `DM` wordmark text, correct font-family/background/overflow), and drove the full click sequence end-to-end on a clean page load — `.scattering` true and `cover-zoom` false at +150ms, `cover-hidden` true and `display:none` by ~+2s, matching the intended two-phase timing. (One interim test run gave a false negative after an unrelated `body.innerHTML += ''` probe wiped all event listeners on the page — identified as a self-inflicted test artifact, not a bug, and confirmed clean on a fresh reload.)
    - **Follow-up same day**: person sent the actual logo file (white-on-black, previous paste had come through blank/white) and asked to match it more closely. Repositioned/added shapes against the clearer reference — pill capsules now have small tick-mark line stubs extending past their rounded ends (top and bottom, matching the reference), and added the two horizontal "connector bar" rectangles bridging the nested-square clusters toward the centre that were skipped in the first pass. Shape count went from 10 to 16 `.cover-shape` elements (all still scatter independently). The wordmark is still bold system text rather than a hand-traced path of the reference's interlocked custom logotype — flagged as the one remaining gap, with person's sign-off to move on regardless ("not to pixel, if it doesn't work we go to next step"). Sent the person a standalone rendered SVG file directly (via file delivery, not a screenshot) since neither the Read tool nor the Browser pane could render a visual preview on this end for self-verification — structural checks (shape count/tags, no console errors) were confirmed, visual fidelity was left for the person to judge from the sent file.
48. **Step 02 — orbs fly into position on entry.** Continuing the staged 10-step rebuild from #47. Previously the orb diagram (About Me + the 3 category orbs) sat at its final resting position from the moment the page loaded — always fully built and animating, just hidden behind the opaque `#cover-screen`. Person asked for the sketch's actual intent instead: orbs visibly flying in from somewhere and settling into place as part of entering the site, not simply being revealed already in position.
    - **Sequencing**: added shared top-level state (`orbIntroActive`, `orbIntroStart`, `triggerOrbIntro()`) right next to the existing `reduceMotion` const, since the cover-screen code and the Three.js orb code are two separate IIFEs inside the *same* `<script>` block — no custom events or `window` globals needed, plain closures reach across. `triggerOrbIntro()` is called from the cover screen's `zoomOut()` (i.e. right as the existing 0.85s `cover-zoom` fade begins, ~520ms after click, once the DM-mark scatter from #47 has finished) — not from the very start of the click — so the orbs are already arriving as the cover fades away rather than popping in after it's gone.
    - **Per-orb entrance descriptor** (`.intro = {startX, startY, startZ, startScale, delay, duration}`): About Me spawns high above its resting spot (`ABOUT_DEF.y + 6.5`) at a fraction of its scale; each of the 3 category orbs spawns even higher and smaller, staggered by `180 + i*160 + random(0,80)`ms so they don't land in lockstep, with randomised (not symmetrical) start-X/start-Z offsets — matching the person's sketch note that positions "shouldn't necessarily be perfectly symmetrical."
    - **`animate()` restructured so position/scale assignment always runs**, blended by an eased 0→1 `introProgress()` (cubic ease-out) between each orb's spawn state and its normal live target — the normal target math (About's float sine wave, each category orb's `curX/curZ/curScale` easing toward its `CAROUSEL_SLOTS` entry) is completely unchanged, just now reached via a `lerp(introStart, target, progress)` instead of being assigned directly. Once `progress` reaches 1 the two are identical, so nothing about the resting diagram changed — verified by sampling orb positions via a temporary debug hook before/during/after triggering entry and confirming the settled values exactly match `ABOUT_DEF`/`CAROUSEL_SLOTS` (within the pre-existing float-wobble range).
    - **Incidental correctness fix in the same block**: position/scale assignment for the category orbs used to happen *only* inside the `if(!reduceMotion)` branch of `animate()` — meaning under reduced motion, `curX/curZ/curScale` were computed correctly but never actually written to `group.position`/`group.scale`, so the category orbs would have silently sat stacked at the scene origin instead of their slots (a latent bug, not something reduced-motion was ever intended to do). Moved position/scale writes for both About and the category orbs outside that guard so they always run; only the floaty sine-bob and continuous rotation stay gated behind `!reduceMotion`, matching how About's positioning already worked implicitly before this change.
    - **Verification**: `canvas.toDataURL()` sampling turned out to be useless for checking WebGL animation progress (the renderer doesn't set `preserveDrawingBuffer`, so reads from outside the paint tick return stale/blank data — not a real bug, just the wrong tool for the job). Switched to a temporary `window.__orbDebug` hook exposing live orb transforms, confirmed the full arc (pre-click: About at y=8/scale 0.12, category orbs small and staggered-not-yet-moved; mid-flight: values interpolating; settled: every orb's x/y/z/scale exactly matching its original resting constants) end to end, then removed the debug hook before finishing. Also re-clicked the centre hotspot after a full settle and confirmed `showCategory` navigation still fires correctly — the `animate()` rewrite didn't regress hit-testing or routing.
49. **Step 03 — hover → zoom + editorial info card.** Continuing the staged rebuild from #47/#48. Sketch showed hovering an orb should enlarge it slightly and reveal a technical/editorial-styled info box beside it (title + short description + "VIEW PROJECTS →"), explicitly *not* a normal tooltip — distinct from the existing `.orb-subflyout` (which already listed sub-filter buttons on hover, built long before this redesign effort, and was left in place unchanged per "modify, don't rebuild").
    - **New `.orb-info-card`** added as a sibling of the existing hotspot/label/subflyout inside each `.orb-slot` (About + all 3 row slots) — dark translucent panel, thin border, a small hollow-square "technical mark," Retron2000 caps typography, connected to the orb by a 1px leader line (`::before` bridging the gap), positioned to the right of left/centre orbs and flipped to the left for the right-hand slot (mirroring how `.orb-subflyout` already flips per column) so it never runs off the side of the diagram. Reveal is pure CSS (`.orb-slot:hover .orb-info-card, .orb-slot:focus-within .orb-info-card{opacity:1}`), same mechanism the subflyout already used, so it's free under `prefers-reduced-motion` (the sitewide `*{transition:none!important}` rule already covers it — no extra guarding needed).
    - **Content is derived, not invented**: `renderInfoCard(el, cat)` (mirrors the existing `renderSubflyout(el, cat)` pattern, called from the same `updateSlotDom()` pass) builds its description line from the real `SUB_CATS` sub-filter labels (e.g. "3D MODELLING · 3D PRINTING · 3D ANIMATION") rather than a made-up tagline. About Me's card is static HTML (no sub-categories to derive from) reusing the real bio location line ("2D/3D Artist — Melbourne, VIC"). The card's CTA button is a real, working control — clicking "View Projects →" calls the same `showCategory(cat,'all')` + `burstParticles()` the orb itself uses on click (About's CTA calls `goAbout()`), not decorative text.
    - **Orb "zoom toward viewer" on hover**: each orb gained `hovering`/`hoverT` state, driven by `pointerenter`/`pointerleave` on its existing DOM hotspot (added alongside the hotspot's pre-existing drag listeners, no conflict — different event types). `animate()` eases `hoverT` toward 0/1 (cubic-ish exponential ease, 0.15/frame, snaps instantly under `prefers-reduced-motion` like the intro/float systems) and applies it as a small multiplicative scale boost (`×(1 + hoverT*0.12)`) plus a forward z-nudge (`+hoverT*0.4` for About, `0.35` for category orbs — camera sits at positive z, so increasing z pulls the orb toward the viewer) on top of the existing intro-blended transform from #48. Category-orb hover is tracked by *slot*, looked up live via `slotAssignment` at hover time (same "live lookup" pattern the existing drag-to-spin code already used) — and `rotateCarousel()` now clears all `hovering` flags when slots reassign, so a hover state can't get stuck "on" for a category that just rotated out from under the cursor.
    - Verified via a temporary `window.__orbDebug` hook: dispatching a synthetic `pointerenter` on the centre row hotspot drove `hoverT` 0→1 and confirmed the resulting `group.scale`/`group.position.z` matched the formulas exactly (`1.2 → 1.344`, `0.35 → 0.7`), then confirmed it eased back to the exact original values on `pointerleave`. Separately confirmed the CSS reveal itself (synthetic pointer events don't trigger real `:hover`, so used `.focus()` instead, which does trigger `:focus-within`): card opacity 0→1, content read correctly ("3D Art" / "3D MODELLING · 3D PRINTING · 3D ANIMATION"), and clicking the CTA navigated to the category screen. Repeated the focus+CTA-click check for the About card (real bio line, `goAbout()` navigation) and confirmed both fired correctly. Removed the debug hook before finishing.
50. **About Me unified into the same scale/behaviour as the category orbs; row given a scattered, non-uniform spread.** Person sent the sketch's panel 3 (four orbs of different sizes floating at different positions/heights) a second time along with a clarifying "reference image 2" (panels 2–3 from the original sheet, re-sent larger/clearer) and asked for About Me to match "same size as the other orbs" and for all four orbs to sit at that reference's positions. Since the sketch shows all four orbs visible simultaneously with no obvious "one is centred/selected" cue, this could have meant replacing the existing swipe-to-rotate carousel (2D/3D/Projects cycling through left/centre/right) with a flat static layout — a large enough behavioural change (removing a deliberately-built, documented interaction) that it wasn't assumed; asked the person directly via a two-option question first. **Person chose to keep the carousel** — so the change made was narrower: resize/reposition About Me and add asymmetric spread to the row, without touching the rotate mechanic itself.
    - **`ABOUT_DEF` changed from `{x:0, y:1.5, radius:2.3125}` to `{x:0.5, y:1.3, radius:1.35}`** — radius roughly halved, landing About's resting scale (now driven by the same intro-blend/hover-blend pipeline as everything else, see #48/#49) close to the carousel centre-slot's effective size (`1.0 × 1.2 scale = 1.2`) rather than dramatically larger. Also nudged off-centre (`x:0.5`) and slightly lower (`y:1.3`), matching the sketch's largest circle not sitting dead-centre. Unlike the earlier #44 spacing fix, this needed **no compensating camera move** — that fix specifically preserved About's *on-screen* size while creating room elsewhere; this one *wants* About's size to visibly change, so `camera.position.z` was left alone.
    - **`CAROUSEL_SLOTS` gained a `y` field per slot** (`left:+0.35, center:0, right:-0.45`, all offsets on top of the existing shared `ROW_Y`) — asymmetric, not mirrored, purely to read as "floating at different heights" rather than a flat row, matching the sketch's scattered composition. Added a matching `curY` state (mirroring the existing `curX`/`curZ`/`curScale` live-easing fields) so a slot's `y` also eases smoothly on carousel rotation instead of snapping — updated in three places: the initial slot-assignment setup, the per-frame `ease` block, and the final position-blend line (`ROW_Y + o.curY + floatY`). `updateSlotDom()`'s DOM hotspot vertical placement updated to match (`(ROW_Y + t.y) / halfHeight`) so the clickable hotspot stays aligned with the visual mesh. Slots remain fully interchangeable — `rotateCarousel()` still just reassigns which category owns which of the 3 fixed slots, unchanged.
    - **Verification hit a real environment gotcha, not a code bug**: the first attempt to verify via a temporary debug hook showed every orb frozen at its intro *spawn* values no matter how long the test waited after clicking the cover screen. Traced it to `document.hidden === true` and the animation loop's `requestAnimationFrame` having fired exactly once, total — the Browser pane isn't actually displayed client-side in this session (consistent with the `computer` screenshot tool's standing "pane not displayed" error throughout this whole build), so the browser correctly suspends rAF for what it treats as a backgrounded tab, regardless of which tab is "selected" among the automation tool's tabs. Confirmed this was the root cause (not a logic bug) by temporarily exposing the `animate` function itself and calling it manually after the same wall-clock wait — with a real frame body actually executing, every value landed exactly on the expected settled numbers (About: `x:0.5, y:≈1.32, scale:1`; 2d/left: `y:≈-2.47, scale:0.55`; 3d/center: `y:≈-2.8, scale:1.2`; projects/right: `y:≈-3.29, scale:0.55`; carousel rotation still reassigns slots correctly afterward). All temporary debug hooks and the frame counter were removed once confirmed — **worth remembering for future verification in this project**: if an animation-dependent check in this preview tab ever looks "stuck," check `document.hidden` before assuming a code regression.
51. **Orb layout rebuilt to trace a "C" curve, per a third sketch reference.** Person sent panel 3 again (four differently-sized orbs at scattered positions/heights) plus, in the same message, re-sent panels 2–3 from the original sheet larger/clearer and asked for the exact positions from that reference, "similar to a C shape." Read the reference as: top-right (smallest) → a medium point → a larger point over on the left → About Me at bottom-right (largest of all four) — a curve that bulges left in the middle with both endpoints on the right, i.e. literally C-shaped, opening rightward.
    - **Checked before assuming**: since all 4 orbs read as simultaneously visible with no obvious "selected" cue (same ambiguity as the previous scattered-layout reference), this could again have meant dropping the swipe/rotate carousel. Didn't re-ask — the person had already answered that exact question two turns earlier (keep the carousel) — so this pass only repositioned/resized things, the rotate mechanic itself untouched.
    - **Every constant became an absolute point instead of an offset**: `ABOUT_DEF` gained a `z` field (`{x:1.02, y:-2.875, z:0.6, radius:1.5}`, the largest and closest-to-camera of the four) and the hardcoded `0` z-target in `animate()`'s About block was swapped for `ABOUT_DEF.z`. `CAROUSEL_SLOTS.left/center/right` each became a full `{x,y,z,scale}` point along the curve (left `{-0.935,-1.0,0.2,1.0}` — largest of the three category orbs, closest to camera; centre `{-0.085,1.5,-0.7,0.75}`; right `{1.53,3.0,-1.5,0.45}` — smallest, furthest back) rather than a shared row + small per-slot nudge. **`ROW_Y` was removed entirely** (three points at three different heights don't have one shared baseline to offset from any more) — updated its three call sites: `updateSlotDom()`'s DOM top-offset formula, the position-blend line in `animate()`, and the category-orb `.intro` spawn height (now a fixed high spawn independent of any slot, since there's no longer a shared row to spawn "above").
    - **Slot→category mapping for on-screen left/right intuition**: sorted the three non-About curve points by x ascending and assigned leftmost→`left` slot, middle→`center`, rightmost→`right`, so drag/swipe direction still corresponds to the orbs' actual left-to-right screen order — deliberately **not** preserving the old "centre = biggest of the three" convention (the curve's size ordering runs top-to-bottom toward About, not by slot identity), since click-to-navigate targets whatever occupies the centre slot by definition regardless of its size.
    - **z depth chosen to reinforce the scale read, not just decorate**: perspective camera means more-negative z (further from the camera at `camera.position.z:12.5`) makes an object read smaller on screen even at the same explicit `scale`, so `right` (smallest, most negative z) and `left`/About (largest, least negative/positive z) get a compounding size cue from depth on top of their literal scale — consistent with how the pre-existing carousel already used z for a "pushed back" read.
    - **Spacing worked out by hand, not derived from the reference's pixel grid** (no tool for extracting exact coordinates from a pasted image) — picked a percentage→world mapping, checked pairwise orb-to-orb distances against the sum of their effective radii (including a margin for the ±0.09 float wobble) before committing numbers, then did one visible-mismatch correction pass: the initial vertical spread read compressed toward the bottom compared to the reference (verified via the DOM hotspot `style.top` percentages, not a screenshot — see below), so `center.y` and `right.y` were pushed up (`0.875→1.5`, `1.7→3.0`) to better use the available frustum height, rechecked for clipping/overlap, and reverified — final hotspot percentages: About `60.5%,83.4%` (ref ≈62%,73%), left `40.4%,61.6%` (ref ≈39%,48%), centre `49.1%,32.6%` (ref ≈49%,23%), right `65.8%,15.1%` (ref ≈68%,12%) — x essentially exact throughout, y close but not pixel-identical, consistent with the person's established "not to pixel" tolerance from the earlier logo pass.
    - Verified via the same manual-`stepAnimate()`-call technique from earlier in this session (the Browser pane still isn't displayed client-side, so rAF still doesn't run on its own): settled `group.position`/`group.scale` for all four orbs matched the new constants exactly, the DOM hotspot percentages above were read directly from `style.left`/`style.top` (not estimated), and `rotateCarousel()` was re-tested afterward to confirm slot reassignment still works correctly against the new coordinates.
52. **"Available for commissions" section and its footer removed from the home screen** — the first feedback in this whole rebuild sent as a real annotated screenshot from the person's own Chrome tab (not this session's preview) rather than a hand sketch: a red circle around the whole contact block plus an arrow-and-X marking it for deletion, with the instruction to keep the home screen focused on just the orb diagram. Confirmed via the HTML structure that `#screen-home` contained the orb diagram, the `.scroll-cue` hint, the `.contact` section (heading, social text-links, email), and a `<footer>` (copyright/location) all nested together, closed by one shared `</section>` — and that `.scroll-cue`/`.contact`/`footer`/`.contact-links` are each used exactly once in the file, only here, so nothing elsewhere depends on them.
    - **Removed the `.scroll-cue` too, not just the section it names** — "Scroll down for commissions & contact ↓" would have been a dead, misleading affordance pointing at content that no longer exists once the section below it is gone, so it was dropped along with it even though the person's circle didn't explicitly extend to that line.
    - **Left the CSS rules in place, only removed the HTML.** Deliberate: the person may want a redesigned version of this content back later as part of the same staged rebuild (the whole effort is explicitly about building a new *interface* around the existing data/content, not deleting content), and unused CSS costs nothing — easy to resurrect, and matches "modify, don't gut" from the very first instructions in this rebuild. If it's confirmed gone for good at some point, the dead `.contact`/`.contact-links`/`.scroll-cue`/`footer` rules would be a reasonable follow-up cleanup.
    - **The embedded CV PDF that lived on the email/social row here (line ~539, ~393KB of base64) was a *separate*, duplicate embed from the CV icon link in the header nav** — confirmed after the edit by grepping for the CV data URI's distinctive prefix: exactly one match remains (the header's), so removing this one didn't touch the still-present header "View CV" icon. File size dropped from ~13.74MB to ~13.31MB as a direct result of dropping that one duplicate embed, not from removing the (much smaller) visible text/links.
    - **Editing mechanic**: same as the FEATURE_LOG #47 cover-screen swap — this range couldn't be matched by the `Edit` tool directly because the embedded-PDF line inside it is too large to include in an exact-match string, so the removal was done as a line-number-based `awk` deletion (skip lines 528 and 530–552, keep 529's `</div>` and 553's `</section>` so the screen's own closing tags survive) after first re-confirming the *current* line numbers directly rather than trusting ones read earlier in the session.
    - Verified via `read_console_messages` (clean) and `get_page_text` (no more "Available for commissions", email, footer, or scroll-cue text anywhere in the rendered output — just the four orb labels/hover-card content), then separately confirmed via `read_page` that the header's YouTube/ArtStation/Instagram/LinkedIn/CV links are all still present and unaffected, since they live in a completely different part of the markup.
53. **"C" curve layout — bottom edge clipping fixed, orb labels pulled clear of the wireframe.** Person sent a real annotated screenshot (from their own Chrome tab) showing About Me's sphere visibly cut off by the bottom of the browser window, plus the orb name labels reading as overlapping/inside the wireframe rather than clearly below it.
    - **Root cause of the clipping**: at the FEATURE_LOG #51 values, About's own radius (1.5) plus its ±0.09 float wobble pushed its bottom edge to roughly y=-4.47, past the camera frustum's visible bottom edge (`halfHeight = camera.z * tan(fov/2) ≈ 12.5 * tan(19°) ≈ 4.3` at the existing `camera.position.z:12.5`/`fov:38°`) — the sphere was being cut against the canvas/frustum boundary itself, not a CSS/DOM clipping issue.
    - **Fix: shifted the whole "C" curve up by a flat +0.55 on every y value**, preserving the curve's exact shape/relative spacing (`ABOUT_DEF.y: -2.875→-2.325`, `CAROUSEL_SLOTS.left.y: -1.0→-0.45`, `.center.y: 1.5→2.05`, `.right.y: 3.0→3.55`) rather than shrinking anything or moving the camera (which would have changed everyone's apparent size, undesired after just tuning those sizes in #51). Checked the new numbers against the frustum bounds before committing, then confirmed after: no orb's DOM hotspot box (top/bottom, computed from the same halfHeight/pxPerUnit math the app itself uses) exceeds the diagram's own 0–760px box in either direction any more.
    - **Label fix was unrelated to the clipping**: `.orb-slot-label{margin-top:12px}` → `26px`. The label was never actually being CSS-clipped (`.orb-diagram` has no `overflow:hidden`) — 12px just wasn't enough visual separation from the glow halo around each orb (the glow sprite extends ~2.7× the orb's own radius), so the text read as sitting inside/on top of the orb rather than clearly below it, especially for the largest orbs.
    - **One tight-but-acceptable tradeoff surfaced during verification**: measuring real `getBoundingClientRect()`s, About's label sits only 26px below its hotspot with ~42px total needed before the bottom of the *diagram's own 760px box* — clears it, but barely, at the diagram's max height. Re-tested at a shorter browser window (700px tall, same width) and found the label (and even part of the orb) pushed below the fold, requiring a scroll — determined this is normal, pre-existing page-scroll behaviour, not a new clipping bug: `.orb-diagram{height:min(92vw,760px)}` sizes off viewport *width*, not height, so a short-but-wide window always could need scrolling here, unrelated to this fix (the resize handler didn't even appear to refire on the test tool's viewport-height-only change, worth rechecking manually in a real browser if this comes up again).
    - Verified via direct `getBoundingClientRect()` comparison (hotspot bottom edge vs. label top/bottom edge, real DOM layout, not estimated) at the default preset viewport: 26px clean gap between orb and label, label fully within the viewport. Cross-checked all four orbs' DOM hotspot top/bottom pixel positions against the diagram's own box height to confirm zero clipping anywhere, not just for About.
54. **Orbs moved up further and spread apart more** — quick follow-up request after #53: still felt too low, and About Me and the "left"-slot orb (the largest of the three category orbs) still read as nearly touching. Checked the actual numbers rather than guessing again: the tightest pairwise margin at the #53 values was About↔left at only 0.209 world units (pairwise 2D distance minus the sum of the two orbs' effective radii) — small enough that the ±0.09 float wobble on each could occasionally bring them within ~0.03 of actually touching.
    - **Pulled the camera back again** (`camera.position.z: 12.5 → 14.5`) — but this time **deliberately did not compensate with a matching position/radius scale-up** the way the #44 fix did. That earlier fix scaled positions/radii by the exact same factor as the camera move specifically to keep About Me's on-screen size/position pixel-identical; here the goal was the opposite — genuinely *more* usable relative space, which a compensated scale-up would have immediately cancelled back out (proportionally scaling the frustum and its contents together produces the identical on-screen result, margins-as-fractions included). The tradeoff: every orb now reads ~14% smaller on screen than before, in exchange for real breathing room.
    - **New absolute positions, still the same "C" shape, hand-tuned and margin-checked before committing**: `ABOUT_DEF: {x:1.1, y:-1.6, radius:1.5}`, `left: {x:-1.3, y:0.3, scale:1.0}`, `center: {x:-0.1, y:2.8, scale:0.75}`, `right: {x:1.8, y:4.1, scale:0.45}`. Every y-value moved up again relative to #53 (About by 0.73, left by 0.75, centre by 0.75, right by 0.55), and the worst-case pairwise margin roughly tripled (About↔left: 0.21→0.60; every other pair already had ≥1.0 margin and stayed comfortably clear).
    - Verified via the same manual-`stepAnimate()` technique (rAF still doesn't run on its own in this session — Browser pane not displayed client-side): computed `halfHeight` from the live `camera.position.z`/`fov` (4.99, up from 4.30), read every orb's settled `group.position`/`group.scale`, and computed all six pairwise distance-minus-combined-radius margins directly rather than eyeballing — smallest is now 0.60 (About↔left), everything else ≥1.0, and both frustum edges (top: `right` orb at 4.57 vs halfHeight 4.99; bottom: `About` at -3.26 vs -4.99) have solid clearance too.
55. **Carousel swap animation removed — instant snap instead of the ~0.5s glide.** Person said to "get rid of the switching effect." Ambiguous enough (could have meant dropping the swipe/rotate interaction entirely, same territory as the #50 carousel-vs-static question) that it was worth a quick check rather than guessing again — asked directly: keep the rotate interaction but make the transition instant, or drop rotation altogether. **Chose: keep rotating, remove only the glide.**
    - **Root cause of the glide**: each category orb's `curX/curY/curZ/curScale` eased 9%-per-frame toward its slot's target inside `animate()` (`o.curX += (t.x - o.curX) * 0.09`, etc.) — an exponential approach that reads as a smooth ~0.5s slide/grow/shrink whenever `rotateCarousel()` reassigned which category owned which slot.
    - **Fix**: `rotateCarousel()` now snaps `orb.curX/curY/curZ/curScale` directly to the new slot's `{x,y,z,scale}` the instant slots are reassigned, instead of leaving them to be discovered and eased toward by `animate()` on subsequent frames. Since `curX` etc. are the *only* thing `animate()`'s per-frame ease block was ever computing, and they're now always already equal to their target the moment a rotation happens, that ease block became dead code — removed it outright (`const ease = 0.09` and the four `+=` lines) rather than leaving working-but-inert math in a hot per-frame loop that could mislead a future read of the code into thinking a glide still happens. The per-orb idle auto-rotation (`rotY`) is untouched — that's continuous spin, not part of "switching."
    - **`.orb-slot.swapping .orb-slot-label{opacity:0}` CSS rule turned out to be dead already** — grepped for where `.swapping` gets added to a slot's `classList` and found nothing; it's unused leftover from an earlier iteration, not something currently firing. Left in place (harmless, unused) rather than touching CSS unrelated to what was asked.
    - Verified with a temporary debug hook: called `rotateCarousel(1)`, then manually stepped **exactly one** animation frame (not several, not a wait) and confirmed every category orb's `group.position`/`group.scale` already matched its new slot's target values exactly (`matchesTargetExactly: true` for x and scale on all three) — proving zero interpolation happens any more, not just "faster" interpolation. Removed the debug hook afterward.
56. **Carousel rotation removed entirely, not just its animation.** Immediate follow-up to #55: "that means the switching overall no need for it." Given the previous message's "switching effect" had turned out to be genuinely ambiguous (animation vs. the whole interaction) and this was the second time in two messages the scope of "switching" needed pinning down, this one was unambiguous — the swipe-to-rotate/click-side-to-select interaction itself is gone now, not just the transition that used to animate it.
    - **Removed**: the `setupCarouselSwipe()` IIFE (swipe anywhere in the diagram to rotate) and the side-slot branch of each row hotspot's click handler (`rotateCarousel(slotName === 'right' ? 1 : -1)`) — a click on **any** of the three row hotspots now calls `showCategory(cat, 'all')` directly, the same thing the centre slot always did, removing the old "2 clicks to enter a side category" behaviour documented back in the original carousel design. The now-uncalled `rotateCarousel()` function itself was deleted rather than left dead in the file.
    - **`slotAssignment` changed from `let` to `const`** — it's set once (`{left:'2d', center:'3d', right:'projects'}`) and, with nothing left to call `rotateCarousel()`, genuinely never reassigned again. Left the rest of the slot/`CAROUSEL_SLOTS` architecture (`categoryOrbs[cat].slot`, `updateSlotDom()`, the `capId()`-based DOM lookups) completely untouched — it already works correctly as a static, one-time-assigned mapping without needing any restructuring, so ripping it out in favour of hardcoded per-category positions would have been a bigger, riskier change for no behavioural difference.
    - **`.orb-diagram{cursor:grab}` and its `:active{cursor:grabbing}` removed** — that whole-diagram grab-cursor affordance existed specifically to hint at the swipe-to-rotate gesture on empty space between orbs; with that gesture gone, hovering empty diagram space now just shows the default cursor. Per-orb `cursor:grab` on `.orb-hotspot` is untouched, since dragging an individual orb to spin it in place is a separate, unrelated interaction that's staying.
    - **Drag-to-spin (Y-axis rotation of an individual orb) was explicitly kept** — the person's phrasing ("the switching") was read as targeting the *category-selection* mechanic specifically, not the general drag interaction on orbs, which doesn't move anything between slots.
    - Verified via direct interaction: clicked the "left" hotspot (permanently `2d`) and confirmed via DOM query it navigated straight to `#screen-category` with `categoryTitle` reading "2D Art" on the very first click — previously this same click would only have rotated `2d` into the centre slot, requiring a second click to actually navigate. Reconfirmed no console errors on a fresh load after removing `rotateCarousel()`, and grepped for any other reference to it (none) before finishing.
57. **Fixed: Projects orb effectively unclickable.** Person reported they "can't access the Projects orb." Traced with `getBoundingClientRect()` rather than guessing: the DOM hotspot `<button>` for the "right" slot (permanently `projects`, and the smallest of the four orbs at `scale:0.45`) measured **68.5px wide but only 21.7px tall** — a thin horizontal sliver instead of the square matching the visually-rendered sphere, so most of the orb a person would actually try to click landed on nothing.
    - **Root cause**: `.orb-slot` is `display:flex; flex-direction:column` containing (in normal flow — `.orb-subflyout`/`.orb-info-card` are `position:absolute` and don't participate) two items: `.orb-hotspot` (`height:100%`) and `.orb-slot-label` (`margin-top:26px` + its own text height, ~42px total). The slot's own height is a **fixed pixel value** set via inline style (`updateSlotDom()`), sized to exactly match the orb's diameter. For the smallest orb, that fixed height (~68.5px) is barely bigger than the label's ~42px need — so default flexbox `flex-shrink:1` kicked in on both children to fit everything inside that fixed box, and proportional shrinking hit the large hotspot far harder than the small label, squashing it down to ~22px. Every slot had *some* degree of this shrinkage (it's proportional to how tight the fit is), but only the smallest orb's was severe enough to actually break clicking.
    - **Why now specifically**: this flex/shrink interaction has existed since the label was first added, but the *previous* message's fix (#54 — camera pulled back to `z:14.5` for more spacing, "every orb reads ~14% smaller") shrank every orb's pixel diameter, including the already-smallest Projects orb, pushing its hotspot from "already a bit squashed but still mostly clickable" to "so squashed it's basically unusable" — a regression introduced by that immediately-preceding change, not a pre-existing separate bug.
    - **Fix**: `flex-shrink:0` on both `.orb-hotspot` and `.orb-slot-label`. Neither item shrinks now — the hotspot always renders at its full intended square size, and the label (which was already established in #53 to sometimes need to render outside the slot's own box) simply overflows below it when there isn't room inside, exactly as it already did for the tightest cases before this bug was introduced. `.orb-slot` has no `overflow:hidden`, so nothing gets clipped by allowing the overflow.
    - Verified by measuring all four hotspots' `getBoundingClientRect()` after the fix: every one is now a perfect square (About 228.3×228.3, left 152.2×152.2, centre 114.2×114.2, right 68.5×68.5 — matching their intended diameters exactly), re-clicked the Projects hotspot and confirmed it navigates to the Projects category on the first click, and re-measured the label gap (still a clean 26px below the now-correctly-sized hotspot, confirming the #53 label fix wasn't disturbed).
58. **Step 04 — click-to-split transition.** Continuing the staged rebuild (#47–#49; #50/#51 handled repositioning, #55/#56 removed the carousel). The sketch's step 4/5 showed clicking an orb breaking it apart (◉ → ◖ ◗) before revealing what's behind it — since the current architecture already has 2D/3D/Projects as three independent, always-visible orbs (not one orb that splits to reveal three panels, which is what the original sketch literally showed), this was scoped down to what actually fits the current design: a break-apart *transition* on the clicked orb itself, playing for a beat before the existing `showCategory()`/`goAbout()` navigation fires, rather than an instant cut.
    - **`splitOrbEffect(orbObj, baseRadius)`** (new, inside the `initOrb()` IIFE so it has `scene`/`THREE` in scope): reads the clicked orb's *current* world position/rotation/scale via `group.getWorldPosition/Quaternion/Scale()` (so the split always starts exactly where the orb visually is, including any live hover-zoom/float offset), immediately hides the real orb group, and spawns two temporary hemisphere wireframe meshes — `THREE.SphereGeometry(radius, 28, 20, phiStart, Math.PI)` with `phiStart` of `-π/2` and `π/2` splits the sphere into screen-right and screen-left halves respectively, matching three.js's phi convention (phi=0 at +X). Both halves get their own `MeshBasicMaterial` clone of the standard wireframe look (same color/wireframe as `makeWireframeMesh`, just not shared since each needs independent fading opacity) and are added directly to `scene` (not as children of the orb's group, since the group is now hidden and the halves need their own independent world-space animation).
    - **A local `requestAnimationFrame` loop** (`stepSplit`, ~460ms, quadratic ease-out) slides the two halves apart along world X (`± baseRadius * worldScale.x * 1.8 * ease`), gives each a small counter-rotating twist, and fades both to transparent — then disposes both temporary geometries/materials, removes them from `scene`, and restores the real orb's visibility. `navigateWithSplit(orbObj, baseRadius, navigateFn)` wraps this: under `prefers-reduced-motion` it just calls `navigateFn()` immediately (no split), otherwise it starts the split and `setTimeout`s the actual navigation to fire once the split's roughly finished — reusing the same reduced-motion branch pattern as the intro/hover systems rather than introducing a new one.
    - **`baseRadius` is deliberately the *unscaled* base radius** (`BASE_ROW_RADIUS` for category orbs, `ABOUT_DEF.radius` for About) — the same value each orb's real wireframe mesh was originally built with — because the temporary halves get `worldScale` applied separately as their own `mesh.scale`, so `baseRadius × worldScale` reproduces the orb's true current on-screen size exactly, the same way the real (hidden) mesh achieves its size via `geometry radius × group.scale`.
    - **Scope decision**: only wired into the two orb-hotspot click handlers (About and the three category orbs), not the `.orb-info-card` "View Projects →"/"View Profile →" CTA buttons added in Step 03 — the sketch specifically depicts *clicking the orb* as the split trigger; the info-card CTA is a separate, later, secondary shortcut, and giving it the same treatment felt like a bigger unrequested addition than the step asked for. Revisit if the person wants the CTA to match.
    - **Follow-up same session**: person noticed the category orbs got the canvas particle burst on click (`burstParticles(e.clientX, e.clientY, cat)`, already wired in before this step) but About Me didn't, and asked for it there too. Added `burstParticles(e.clientX, e.clientY, 'about')` right before `navigateWithSplit(...)` in the About hotspot's click handler. Deliberately used the string `'about'` as the mode rather than `'all'` — `makeParticle()`'s shape/colour switch has explicit cases only for `'3d'`/`'2d'`/`'projects'`, with everything else (including `'all'` and now `'about'`) falling through to the same mixed-shape default; but colour is gated separately (`isGold = mode === 'all'`), so `'about'` gets the same mixed square/triangle/cube/line shapes as `'all'` while staying white/ivory rather than gold — required, not incidental, since DESIGN_SYSTEM.md's gold-is-"All"-only rule was corrected once already after an earlier mistake (see CLAUDE.md's Design System section) and a literal `'all'` here would have silently violated it. Not added to the About "View Profile →" CTA button, matching the same orb-click-only scope already established for the split effect above. Verified by dispatching a real click event (with `clientX`/`clientY` set, since `burstParticles` reads them off the event) at the hotspot's centre and confirming no exception and correct navigation.
59. **Step 05 — category screen's sub-filter row redesigned as editorial vertical panels.** Continuing the staged rebuild. The original written spec's step 6 showed a horizontal row of tall narrow panel cards for category selection (dark translucent bg, thin borders, gradients, subtle noise, technical labels, vertical/editorial typography) — explicitly *not* the plain pill-shaped chip buttons the category screen used until now. Scoped to the current architecture the same way Step 04 was: since the top-level 2D/3D/Projects choice already happens via the three separate orbs on the home screen (not a single splitting orb revealing 2D/3D/Projects as described in the original mock), the panel treatment was applied to the **sub-category row within a category screen** (e.g. Concept Art / Character Art / Graphic Design inside 2D) — the row that's actually analogous to "category selection" in the current structure — rather than inventing a new intermediate screen.
    - **New `.cat-panel` replaces `.sub-filter-btn` for the sub-filter row only.** Each panel: 112×184px (narrow, tall — "large spacing" achieved via a 16px gap and generous internal padding rather than cramming pills together), background is a diagonal gradient over the existing `--card` token (`linear-gradient(160deg, rgba(255,255,255,.07), transparent 55%)`, `rgba(255,255,255,.14)` intensified for the active state using `--accent` instead), 1px `--line` border, and a `::after` pseudo-element carrying a small self-contained noise texture (inline `feTurbulence` SVG data-URI, `mix-blend-mode:overlay` at 5% opacity — no external asset, matches the "subtle distortion/noise" ask without adding a file). Content per panel: a small `"01 / 04"`-style index/count label top, the sub-category name rotated vertical (`writing-mode:vertical-rl; transform:rotate(180deg)`) as the main content, and a small hollow square mark bottom-right reusing the same particle-burst shape vocabulary (squares/triangles/lines) as a "technical" flourish. Active state swaps the border/label to `--accent` and fills the little square mark solid.
    - **The Year row (Projects screen only) was deliberately left as the old `.sub-filter-btn` pills**, not given the same panel treatment — it's a secondary refinement within an already-chosen sub-category, not the primary "category interface" the reference was describing, so promoting it to the same visual weight would have overstated its importance in the hierarchy.
    - **All existing behaviour preserved exactly** — `renderCategoryFilters()`'s click handler logic (`state.sub = sub; renderCategoryFilters(cat); renderCategoryGrid(); burstParticles(...)`) is completely unchanged, only the DOM/CSS built for each option changed from a `<button class="sub-filter-btn">` to a `<button class="cat-panel">` with three child spans instead of a single text node.
    - Verified via direct interaction: navigated into 3D Art and confirmed all 4 panels render with correct index labels ("01/04"–"04/04") and the right one starts active; clicked the second panel ("3D Modelling") and confirmed via DOM query it became the *only* active panel and the work grid actually re-filtered (8 items → 6); checked computed styles directly (112×184px, `writing-mode:vertical-rl`, correct ink colour, noise pseudo-element present) rather than assuming the CSS took effect; separately navigated to Projects and confirmed the Year row still renders as the old simple pill style, untouched.
60. **Step 06 — project grid cards given the same editorial framing as the Step 05 panels.** "Category selection → project interface" in the original plan; interpreted as: once a category/sub-category is chosen, the actual grid of project thumbnails that appears should read as part of the same visual system as the new panels, not switch back to a plain image grid. Kept intentionally light-touch compared to #59 — these are photo/video thumbnails, not empty panels, so the change is a technical *frame* around each image rather than a rebuild of the card.
    - **`.work-card` gained a 1px `--line` border** (brightening to `--muted` on hover, matching the panel hover treatment) and the grid gap widened `6px→14px` so the new borders read as distinct cards with breathing room rather than a tight contact sheet.
    - **Two small always-present overlay elements per card, both `pointer-events:none` so they never interfere with the existing click-to-open-detail behaviour**: `.work-index` (top-left, the same `"01 / 05"`-style position/count label used on the Step 05 panels, at low opacity that brightens on hover) and `.work-card-mark` (top-right, a small hollow square that fills solid `--accent` on hover — reusing the same shape vocabulary as the panels' corner mark and the particle-burst system). Both sit at `z-index:2`, above the image but not conflicting with the existing centred video play-button pseudo-elements (`::before`/`::after` on `[data-video]`), which stay centred and untouched.
    - **The existing hover-reveal title/caption overlay (`.work-meta`) was left completely alone** — it already worked well and wasn't part of what felt "conventional," only the plain borderless card frame was.
    - **Scope boundary**: only the grid/thumbnail view was touched, not the individual detail/content page (`#screen-detail`) that opens when you click a piece — that's a separate, larger surface (title, description, gallery thumbnails, prev/next) that would warrant its own pass rather than a same-turn tack-on; flagged as follow-up if wanted.
    - Verified via direct interaction: navigated into 2D Art (5 pieces after the default sub-filter), confirmed all 5 cards show correct sequential index labels ("01/05"–"05/05" — count-relative to the *currently filtered* list, matching how `state.list`/detail prev-next already worked) and the corner mark, checked the border colour resolves to the `--line` token, then clicked the first card and confirmed it still opens `#screen-detail` with the correct title ("Still Kitchen") — the new overlay elements don't block the click.
61. **Step 07 — "half-orb" back control that reassembles before returning home.** The original sketch's step 7 was explicit: clicking a split orb's half should visibly reassemble it, then return to the floating-orb home state — "much more interesting than a normal back button." Implemented as a small reusable icon + behaviour on the two back buttons that actually *go home* (category and about screens) — the detail screen's back button goes to the category screen, not home, so it stays a plain conventional button, untouched.
    - **`.back-orb`**: two absolutely-positioned, identically-sized circles (`.back-orb-left`/`.back-orb-right`) each `clip-path: inset()` down to just their half, sitting a couple pixels apart at rest (`translateX(±2.5px)`) so the button reads as "two halves of a wireframe orb" rather than a solid circle. Hovering nudges them slightly further apart (`±4px`) — a small tell that they're separate, splittable pieces, echoing the Step 04 split effect's shape language without needing real 3D. Pure CSS, no JS needed for the visual at rest/hover.
    - **On click**: `reassembleAndGoHome(btn)` adds a `.reassembling` class (both halves `transform:translateX(0)`, snapping together over the existing `0.35s` transition), waits `360ms` — just past the transition's own duration — then removes the class and calls the existing `goHome()`. Under `prefers-reduced-motion` this skips straight to `goHome()`, same branch pattern used everywhere else motion was added this build.
    - **Icon markup is inline, duplicated on both buttons** (`<span class="back-orb"><span class="back-orb-half back-orb-left"></span><span class="back-orb-half back-orb-right"></span></span>` before the "Home" text) rather than templated, matching how small repeated bits of markup are already handled elsewhere in this file (e.g. the header's nav icon `<svg>`s) — the file has no component/templating system to share it through.
    - Verified via direct interaction and computed-style checks: confirmed the left half's resting `transform` is the expected `matrix(1,0,0,1,-2.5,0)`; clicked the category back button and confirmed `#screen-home` was still inactive 100ms later (`.reassembling` present) but active ~600ms later (class removed) — genuinely delayed, not instant; repeated the same check for the about back button; and confirmed the detail screen's back button has **no** `.back-orb` and navigates in ~20ms with no delay, proving the scope boundary held.
62. **Step 08 — subtle mouse-trail dust.** The sketch was explicit about *not* wanting a normal glowing cursor trail — "a field of small points following the mouse movement... subtle, not distracting... particles being disturbed by the cursor," sitewide (not scoped to one screen).
    - **New `dust` array, sharing the existing `#particle-canvas`/`ctx`/`stepParticles()` loop** rather than standing up a second canvas or a second `requestAnimationFrame` loop — `stepParticles()` already clears and redraws that canvas every frame for the click-burst system, so folding dust into the same pass avoids two loops fighting over one canvas's clear/draw cycle. The loop's continue-condition changed from `particles.length > 0` to `particles.length > 0 || dust.length > 0`, so it now also stays alive purely from mouse movement, independent of any click burst.
    - **Deliberately different visual language from the click-burst particles** — small filled circles (`drawDust()`, plain `ctx.arc` fill) instead of the burst system's outlined squares/triangles/cubes/lines, low fixed max opacity (`life * 0.5`), always the plain white/ivory colour (never gold — gold is reserved for the "All" filter's burst, unrelated to this), tiny size (1–2.8px radius), and slow drift (speed `0.15–0.45`, plus per-frame `×0.98` drag) — reads as fine disturbed dust, not a repeat of the burst effect.
    - **Spawn is distance-throttled, not per-event**: a `pointermove` listener only spawns when the cursor has moved ≥8px (`dx²+dy²<64` check) from the last spawn point, adding 1–2 dots per qualifying move with a small random jitter/velocity — `mousemove` fires far more often than 8px of real movement, so this keeps the dot field looking like discrete "clusters" (matching the sketch's `•` / `• •` / `• • •` clustering) rather than a continuous smear, and keeps the spawn rate cheap. The pool is hard-capped at 140 (`dust.splice()` trims the oldest when exceeded) as a simple safety bound against runaway growth during long mouse sessions.
    - **Respects `prefers-reduced-motion`**: the whole `pointermove` listener is only attached `if(!reduceMotion)` — under reduced motion there's no dust at all, rather than spawning-but-not-animating (matches how motion is skipped everywhere else in this build, not added-then-frozen).
    - **Verification hit the now-familiar backgrounded-tab timer throttling, more severely than in earlier steps**: this time even `window.requestAnimationFrame` monkey-patched to `setTimeout(fn,16)` only progressed a handful of "frames" over 3 real seconds (rafId incremented 1→6, nowhere near the ~30 frames real 16ms cadence would produce) — meaning even *timers*, not just rAF, are throttled hard in this specific backgrounded/non-displayed tab, not just compositor-tied rAF. Confirmed the parts that don't depend on real-time frame cadence instead: dispatched synthetic `pointermove` events ≥8px apart and confirmed `dust.length` grew correctly (14 dots from 10 qualifying moves) and `rafId` went from `null` to a real timer id (loop started), then removed the temporary `window.__dustDebug` hook. The decay/movement math itself (`life -= decay`, `x += vx` etc.) is simple, already-proven arithmetic (same pattern as the existing burst particles) rather than something novel needing its own from-scratch runtime proof. Separately re-confirmed the existing click-burst-into-navigation flow (About → Projects) still completes without error, since it shares the same loop this step modified.
63. **Step 09 — scramble/decode text reveal on the home orb labels.** Of the two typography references the person gave earlier in this project ("typography/editorial styling" as a generic 10-step-plan line item, and a specific garbled-text-in-a-box reference image with the explicit note "i want this simular effect within loading the orbs showing the categories but not when you enter the orbs only the main user face... keep the colour similar to claude code art"), the second was concrete enough to implement directly rather than attempt a vague sitewide "editorial pass": a brief scramble-to-real-text animation on the orb labels (About Me / 2D Art / 3D Art / Projects) as they fly in, and *only* there.
    - **`scrambleText(el, duration)`** (new top-level function, next to `triggerOrbIntro`): captures the element's current `textContent` as the target, then on each frame reveals characters left-to-right in order while everything not yet revealed is replaced with a random character from a fixed technical-looking set (`ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789/#_` — uppercase/digits/a few symbols, matching the blocky look of the reference image rather than lowercase prose). Spaces are always left alone (never scrambled) so multi-word labels like "about me" still read as word-shaped while resolving. Skips entirely under `prefers-reduced-motion` (labels just stay correct, never scrambled).
    - **"Keep the colour similar to claude code art" was satisfied by *not* touching colour at all** — the function only ever rewrites `el.textContent`; the label's existing CSS (`color:var(--muted)`, `var(--accent)` on hover) is completely unaffected, so the scrambled characters render in the exact same established palette rather than introducing a new "terminal green" or similar look.
    - **Wired into `triggerOrbIntro()`** (`document.querySelectorAll('.orb-diagram .orb-slot-label').forEach(el => scrambleText(el, 600))`), which already fires exactly once, at the start of the cover screen's zoom-out — so the scramble plays during the fly-in, once per full page load, and **only** targets `.orb-slot-label` elements inside `.orb-diagram`, i.e. only the four home-screen orb labels. Nothing on the category screen (`#categoryTitle`, `.cat-panel-label`, etc.) or detail screen was touched, matching "not when you enter the orbs only the main user face" directly — those elements aren't even selected, not merely styled differently.
    - **Verification needed a different approach than earlier steps** because of how badly timer-throttled this backgrounded tab has proven across the whole session: chaining `await`s *across separate tool calls* turned out to be unreliable here specifically because real wall-clock time elapses between one tool call ending and the next one starting (LLM/tool-orchestration latency), which is enough time for a ~600ms animation to fully resolve before the next call even checks it — an initial multi-call test misleadingly looked like "nothing happened" for exactly this reason. Switched to single-call, self-contained tests instead: exposed `scrambleText` via a temporary `window.__scrambleDebug` hook and (a) called it directly and checked `el.textContent` *synchronously in the same statement* (proved the first frame runs inline and immediately scrambles — no timer dependency for this part at all), (b) ran it across all four real labels at once and confirmed all four scrambled with correct per-label lengths/space-preservation, and (c) within one `async` call, monkey-patched `requestAnimationFrame` to `setTimeout(fn,16)`, triggered a scramble, checked the text was scrambled immediately, `await`ed past the duration *within that same call*, and confirmed it resolved to the exact original text. Removed the debug hook once confirmed.
64. **Step 10 — responsive/mobile pass, the last of the 10-step plan.** Unlike earlier steps this wasn't scoped from a single sketch reference — it was CLAUDE.md's long-standing "no mobile-specific pass on the orb diagram... hasn't been tested/tuned" gap, so the approach was to actually resize the preview to a real phone width (375×812) and look for genuine breakage rather than guess. Found three real bugs, all pre-existing or introduced by earlier steps in this rebuild, not by this pass itself:
    - **Sitewide horizontal scroll on mobile.** Root cause: the Step 03 `.orb-info-card` hover panels are `position:absolute; left:100%` relative to their orb slot — invisible at rest (`opacity:0`) but still occupying layout space, and on a 375px-wide screen an orb positioned anywhere near the right two-thirds of the diagram pushes its (invisible) info-card past the true viewport edge. `body{overflow-x:hidden}` alone didn't stop this — confirmed via `document.documentElement.scrollWidth` (511px) exceeding `clientWidth` (375px), and `window.innerWidth` itself had inflated to 511 to accommodate the overflowing content. **Fix**: added `overflow-x:hidden` to `html` as well as `body` — the standard cross-browser-safe pairing for this exact class of bug (absolutely-positioned descendants bleeding past the viewport despite the body's own overflow rule). Verified `scrollWidth` dropped to exactly match `clientWidth` (375=375) afterward.
    - **Category screen missing its horizontal padding, at every viewport width, not just mobile.** Found while investigating why the sub-filter panels looked flush against the screen edge on mobile — turned out `.section{padding:60px 0 110px}` (a padding *shorthand*, which resets all four sides) was silently overriding `.wrap{padding:0 40px}`'s left/right padding, since both classes sit on the same `<div class="wrap section">` for the category screen and `.section` is declared later in the stylesheet (equal specificity, later rule wins on the shared `padding` property). Confirmed via `getComputedStyle` that `paddingLeft`/`paddingRight` were `0px` at *both* a mobile and a full desktop viewport — this bug predates this session's work and wasn't mobile-specific at all, just more visually obvious once panels started touching a narrow screen's edge. **Fix**: changed `.section` to `padding-top`/`padding-bottom` only (no shorthand), so it no longer touches left/right at all — confirmed `.section` is used in exactly one place in the file (`class="wrap section"` on the category screen), so this was safe with no other call sites to check. Reconfirmed 40px padding on both sides at both viewport sizes afterward.
    - **Orb diagram overlap and undersized tap targets on narrow/portrait viewports.** The "C" curve's world positions (FEATURE_LOG #51/#53/#54) were tuned and verified only ever against a landscape-ish aspect ratio; on an actual phone-portrait viewport, `.orb-diagram`'s aspect ratio drops well below 1 (0.66–0.87 depending on how the existing `min-height:520px` interacts with the 900px breakpoint's `height:min(105vw,560px)` — the two together were forcing an even taller, narrower box than the vw-based height alone would), and since `halfWidth = halfHeight × aspect`, horizontal room shrinks much faster than vertical room as aspect drops, producing a small real overlap between the About and left-slot hotspots (dozens of pixels, not huge, but real). Two-part fix, arrived at after an over-correction that had to be walked back:
      - First attempt pulled the camera back based on a "reference aspect" of `1100/760 ≈ 1.45` (the orb-diagram's own *maximum* aspect at its widest). This was wrong on two counts, both caught by re-checking rather than trusting the first result: (a) even at a substantial capped pull-back (1.55×) the overlap *still* wasn't fully resolved, while tap targets shrank to as little as 23px — worse than before on the metric that actually matters (usability); and (b) re-testing at a normal desktop width afterward found the "desktop" preset renders the diagram at aspect ≈1.13 (narrower than its 1100px max-width in practice), which is *below* 1.45 — meaning the "mobile-only" pull-back was silently engaging on ordinary desktop windows too, a regression from the #54 baseline that hadn't been caught until specifically re-verified.
      - **Final fix**: (1) lowered `.orb-diagram`'s mobile `min-height` from the inherited 520px to 380px, so the box's aspect ratio on phones is governed mostly by the intentional `105vw` sizing rather than an unrelated floor fighting it; (2) replaced the "reference aspect" comparison with a simple `aspect < 1` (portrait) threshold — real desktop/landscape windows are reliably aspect > 1, real mobile portrait is reliably aspect < 1, so this needs no calibration against any particular container width and can't misfire on desktop the way the max-width-derived ratio did; capped at 1.2× pull-back (mild, not the earlier 1.55×); (3) added a 44px (standard minimum tap-target size) floor to both hotspot-sizing calculations (`updateSlotDom()` for category orbs, `resize()` for About) — the *visual* orb can still render smaller than that on a phone, but the invisible clickable area around it never does. Net result: a residual overlap of 3.3×19.8px between two hotspots' *bounding boxes* (≈1.5% of the smaller one's area, and likely zero actual overlap once the circular hit-shape is accounted for, since box corners touching doesn't mean the inscribed circles do) — accepted as below any real usability threshold rather than chased further, especially since a direct click test confirmed both orbs still route correctly.
    - Verified each fix in isolation and then together: re-checked `document.documentElement.scrollWidth` vs `clientWidth` after each change (all zero-overflow), re-measured `.wrap` padding on both the category and confirmed-unaffected screens, re-measured all four hotspot dimensions (44–103px, no more sub-25px targets), reconfirmed `camera.position.z` returns to exactly the baseline `14.5` at desktop aspect (no regression) while correctly engaging a modest `~16.7` at phone aspect, and ran full click-through flows (home → About → back, home → category → detail) at both viewport sizes with `read_console_messages` checked clean after every reload.
65. **About Me nudged right; 2D/3D swapped which slot they occupy.** Person sent a real annotated screenshot from their own browser circling the gap between About Me and the left-slot orb and asking for more breathing room ("not too close"), plus a swap of 2D Art and 3D Art's positions.
    - **`ABOUT_DEF.x: 1.1 → 1.6`** — a modest rightward nudge (y/z/radius untouched), pulling About further from the left slot (`x:-1.3`) without needing any of the camera/aspect machinery from #64 — this is a straightforward world-position change, not a spacing-system change.
    - **`slotAssignment` swapped from `{left:'2d', center:'3d', ...}` to `{left:'3d', center:'2d', ...}`** — since it's a `const` set once (the carousel was removed in #56), this is a one-line static swap, not a runtime rotation. 3D Art now renders at the larger, closer-to-camera "left" point on the curve and 2D Art at the medium "centre" point; Projects/"right" is unchanged.
    - **Nice side effect**: re-checked the ~3×20px residual hotspot overlap flagged as an accepted gap in #64 (About vs. the left-slot orb, only present at the narrowest phone width) — the rightward nudge alone fully closed it; `overlapPairs` came back empty on a fresh mobile-width check. Worth remembering if `ABOUT_DEF.x` is ever tuned back down.
    - Verified via direct interaction: read the row labels post-scramble-trigger (confirmed via a `catTitle` check rather than trusting the label text, since Step 09's scramble was mid-animation at check time in this rAF-suspended tab) — clicking the "left" hotspot now opens **3D Art**, not 2D Art, confirming the swap took effect functionally, not just in a config object nobody reads. Measured a clean 30px gap between About and the left orb at desktop, and re-ran the mobile overlap check from #64 to confirm it's now empty.
66. **About page: new bio copy, an Education section, and a Contact section.** Person asked directly for three additions to `#screen-about`.
    - **Bio replaced** — from "I'm a 2D and 3D artist with a passion for..." to a longer "2D/3D Generalist" version the person supplied verbatim, describing both the artistic-direction/craft list and the cross-2D/3D-workflow angle. `.about-text p:first-child` styling (26px, weight 300, `--ink`) is untouched, just the text content.
    - **New `.education-list`** (four items: Master of Animation Games & Interactivity, Bachelor of Animation, Advanced Diploma of Screen and Media, Certificate III in Design Fundamentals) — normalised the casing/wording the person typed in chat (mixed case, one item missing a preposition — "Advanced diploma screen and media") to consistent Title Case with `of`/`in` filled in grammatically, rather than pasting the raw chat text verbatim; didn't invent any quals, just cleaned up presentation of what was given. Styled as a plain hairline-divided list (`border-top` per `<li>`, matching the sitewide "hairline dividers, not filled ones" convention from DESIGN_SYSTEM.md) rather than the pill-tag style used for `.medium-list` — full qualification names don't read well as pills the way short tool names do.
    - **New Contact section reusing the existing `.contact-links` CSS** — this class (and `.contact-links a`'s underline-link styling) has been sitting unused in the file since FEATURE_LOG #52 removed the home-page "Available for commissions" section that originally used it; explicitly kept then "in case this content comes back in a redesigned form," which is exactly what happened here. No new CSS needed for the links themselves, just a new small `.about-section-label` style (reused the same small-caps/`--muted`/`opacity:0.6` treatment as the category screen's `.filter-row-label`) for the "Education"/"Contact" headers above each block.
    - **The gmail address is back, and the old "don't re-add it" guidance is now stale.** CLAUDE.md previously recorded that a second Gmail address had been intentionally removed from the site per an earlier request and should stay off. This message explicitly asked to add `dinethmeehi@gmail.com` back (alongside the Hotmail address) — a direct, current instruction about the person's own contact info, which supersedes the earlier removal. **Updated CLAUDE.md's "Who this is for" section to drop the outdated "don't re-add" note** rather than leaving two contradictory instructions in the file for a future session to trip over.
    - **Corrected a typo, didn't propagate it**: the person's message had the Hotmail address as `dmeehitiya@hotmaill.com` (double L) — every other record of this address across the whole project (CLAUDE.md, the original CV/contact work) spells it with one L, `hotmail.com`. Used the correct, established spelling rather than the typo, since a `mailto:` link to a misspelled domain would just be a broken link — flagging this correction here rather than silently guessing.
    - Verified via direct interaction: navigated to the About screen and read back the actual rendered `textContent`/`href` values (not just checking the edit applied) — bio text matches exactly, all four education items present in order, both contact links present with correct `mailto:` hrefs using the corrected spelling, and both new section labels ("Education", "Contact") render. Spot-checked computed styles (hairline border colour resolves to the `--line` token, link underline present) rather than assuming the reused CSS classes still applied correctly after sitting unused since #52.
    - **Immediate follow-up, same session**: person's next message ("get rid of the old bio") turned out — clarified by a follow-up annotated screenshot with "BIO" and "SKILL" handwritten in red above the paragraph and tool tags — to mean "give Bio and Skills their own section labels too, matching Education/Contact," not "delete bio content" (the old bio text had already been fully replaced in the edit above; confirmed via grep before responding that it didn't exist anywhere in `portfolio_v2.html`, and that it *does* still exist in `portfolio.html`/v1, which this whole session has left untouched — flagged both facts back to the person rather than guessing which file they meant). Also asked to make the section labels themselves "bold and visible."
      - Added two new `.about-section-label` elements: "Bio" directly before the bio `<p>`, "Skills" directly before `.medium-list` — same element/class already used for "Education"/"Contact", just two more of them.
      - **`.about-section-label` restyled from a subtle caption to an actual heading**: `font-size:10px→15px`, `opacity:0.6→1`, `color:var(--muted)→var(--ink)`, added `font-weight:700` (synthetic bold — Retron2000 only has one real weight, same synthetic-bold technique already used elsewhere in the file). This is a shared class, so Education and Contact's labels picked up the bolder look automatically without touching their markup.
      - **`.about-section-label:first-child{margin-top:0}`** — the base rule's `margin:36px 0 12px` is meant to separate a label from the section *above* it; without this override the new leading "Bio" label (now the very first child of `.about-text`) would have carried that same 36px gap as leftover space at the top of the whole about column, which reads as a layout mistake rather than intentional spacing.
      - Verified via direct DOM/computed-style query: all four labels ("Bio", "Skills", "Education", "Contact") present in the correct order, all reporting `font-weight:700`, `opacity:1`, and `--ink`'s RGB value; confirmed the first label's `margin-top` resolves to `0px` specifically (not just eyeballing whether it looked right).
    - **"Commissions Open" removed from the Skills tag list** — it had been mixed in among the real tool names (Maya/ZBrush/etc.) since v1, which reads oddly now that "Skills" is an explicit, bold section label rather than an unlabelled tag row. Skills is now exactly the 7 real tools. Verified the remaining list via direct DOM query.
    - **Bio switched from first-person to third-person** ("I'm a..."/"I enjoy..." → "Dineth Meehitiya is a..."/"He enjoys...") — same content/wording otherwise, person supplied the exact replacement text. Verified via direct `textContent` read.
    - **Same-day follow-up tightened it further**: "He enjoys working across... from initial ideas through to final production" → "He works across... to develop engaging visuals, characters, environments, and creative concepts." (dropped "enjoys" and the "from initial ideas through to final production" close). Verified via direct `textContent` read again.
67. **Header `.nav-logo` icon removed.** Person sent a real annotated screenshot circling the small circular logo mark that sits next to "Dineth Meehitiya" in the header and asked to remove it. Removed the `<img class="nav-logo" ...>` element (the embedded `Logo/Logo DM.png`) entirely, leaving `.nav-name` as plain text — the `.nav-logo` CSS rule (`height:24px`) was left in place, unused, matching how other now-orphaned CSS from earlier removals in this project has been handled (cheap to leave, easy to resurrect if the person wants a mark back later in a different form).
    - **Editing mechanic**: same as every other edit touching this line all session — `<div class="nav-name">Dineth Meehitiya<img ... src="data:image/png;base64,...">...</div>` is one single ~78KB line (the embedded PNG), too large for the `Edit` tool's exact-match requirement, so this was a line-number-based `awk` splice (re-confirmed the current line number immediately before splicing, not reused from an earlier read) — replaced the whole line with a plain `<div class="nav-name">Dineth Meehitiya</div>`. File size dropped by exactly the size of the removed image (~78KB).
    - **Unrelated hiccup during verification, not a code issue**: the local PowerShell preview server (`serve.ps1`, started once, several turns earlier in this session) had stopped running by this point — `preview_list` came back empty. Restarted it via the existing `.claude/launch.json` "portfolio-v2" config; it came back up on a different port (5173 instead of the original 8734, whatever was free this time) with no other change needed. Worth remembering: if a future turn's first navigation attempt in this project fails, check `preview_list` for an empty process array before assuming a code regression — the server itself may just need restarting, and the port can differ between restarts.
    - Verified via direct DOM query: `.nav-logo` no longer exists in the document, `.nav-name`'s `innerHTML` is exactly the plain text with no leftover tags, and a full cover→About navigation flow (including the FEATURE_LOG #66 bio text) still completes correctly, confirming the header edit didn't disturb anything downstream.
    - **Verification hit the same backgrounded-tab rAF issue as before** — the split's own internal `requestAnimationFrame` loop is just as susceptible to the Browser pane not being displayed client-side as `animate()` was in earlier steps, so a first check showed the two temporary hemisphere meshes added to the scene (children 8→10) and the real orb correctly hidden, but then stuck there indefinitely instead of cleaning up. Confirmed this was the same environment artifact (not a new bug) by temporarily monkey-patching `window.requestAnimationFrame` to run via `setTimeout` instead for one test — with the loop actually able to run, the scene correctly returned to 8 children and the orb's visibility was correctly restored well within the wait window. Separately confirmed via real wall-clock timing (no monkey-patch) that navigation is genuinely delayed and not instant: `#screen-category` was still inactive 150ms after an orb click and became active only after the full ~460ms had elapsed, for both a category orb (Projects) and About.

68. **Detail-screen showcase images made zoomable — the crude `.lightbox` rebuilt into a real image viewer.** Person's ask: the showcase images felt "too small" and not interactive; wanted clicking one to open a larger view, on the same page, with a smooth open animation, the ability to zoom in to inspect artwork / 3D-model renders, and an easy close. Explicitly said to keep the current visual style and layout and *only* improve image viewing — so nothing about `.detail-wrap`, the grid, or the thumbnail strip's size/position changed.
    - **What the old `.lightbox` was**: a bare click-anywhere-to-close overlay (`rgba(5,5,6,0.92)`, one `<img>` at `object-fit:contain`, no zoom, no animation), only ever reached from a *video* piece's gallery thumbnail (image pieces swap `#detailImage` in place and never opened it). `openLightbox(src, alt)` was the whole API.
    - **What it is now** — same `#lightbox` id and `z-index:400`, rebuilt in place (CSS block, the HTML element, and the JS controller that replaced the old 13-line one):
      - **Chrome, all in the site's hairline/Retron idiom** (transparent fills, `1px var(--line)` borders, `var(--muted)`→`var(--ink)` on hover — no new colours): a `&times;` close mark top-right, a `NN / NN` count top-left, `&#8249;`/`&#8250;` prev/next arrows mid-edge (hidden unless the piece has >1 image), and a `− / Reset / +` control row bottom-centre. Backdrop bumped to `rgba(6,6,7,0.975)` (near-opaque) so dense reference sheets read cleanly with no page bleed-through.
      - **Open animation**: backdrop opacity fades (`.shown`), image does a `scale(0.94)→1` ease via `cubic-bezier(0.22,1,0.36,1)` on an rAF after the `.active`/reflow/`.shown` sequence. Under `prefers-reduced-motion` it skips straight to `scale(1)` and `closeLightbox()` runs its cleanup synchronously (the global `*{transition:none!important}` rule covers the CSS side).
      - **Zoom** (`LB` state object holds `scale` + px pan `tx/ty`, `min:1 max:6`): mouse wheel (zooms toward the cursor via a fixed-point transform — `tx = ox - (ox - tx)*k`), two-finger pinch (`touch-action:none` on the stage; midpoint is the focal point), the `+`/`−`/`Reset` buttons, and `+`/`-`/`0` keys. A plain tap/click **on the image** toggles between fit and 2.5× at the clicked point; a tap on the surrounding backdrop closes.
      - **Pan**: drag when zoomed (pointer events, `setPointerCapture` on the stage). `lbClampPan()` limits translation to half the scaled image's overflow past the stage on each axis, using `offsetWidth/Height` (layout box, transform-independent) so the image can't be dragged off-screen.
      - **Navigate without leaving**: for a piece with a gallery, the viewer steps through `[item.full, ...item.gallery]` via the arrows or `←`/`→`; each step resets zoom. Counter shows position.
      - **Close**: `×`, backdrop tap, or `Esc`. On close the body scroll-lock (`body.lightbox-open{overflow:hidden}`) is released and `img src` is cleared (frees the multi-MB base64 string).
    - **Wiring into `renderDetail()`**: `allImages` is now computed once near the top and shared by the thumbnail strip and the viewer. The **main `#detailImage` (image pieces) is now click-to-open** (`cursor:zoom-in` added to `.detail-media img`), opening the viewer at whichever gallery image is currently shown (`currentImageIdx`, updated by the thumb handler). **Thumbnail behaviour is unchanged**: image pieces still swap the main image in place (front image `allImages[0]` never changes — the gallery rule holds); video pieces still open the viewer from a thumb, just now with zoom + arrows instead of the old flat overlay. `#screen-detail`'s layout/markup is otherwise untouched — still flagged in CLAUDE.md as not having had an editorial pass.
    - **Two robustness fixes made during testing**: (1) `closeLightbox()`'s `transitionend` cleanup now guards `e.target === lightboxEl` — otherwise a `transitionend` bubbling up from the image's own transform/zoom animation could complete the close prematurely. (2) tap-vs-drag / image-vs-backdrop detection reads `LB.downOnImg` captured at `pointerdown` (target is reliable there) rather than hit-testing `getBoundingClientRect()` at `pointerup`, which is wrong mid-zoom-animation and mis-targeted once `setPointerCapture` retargets the `pointerup` to the stage.
    - **Verified** on the live preview: opens from a Keys (image, 3-img gallery) main image and from a Ravager (video piece, dense reference-sheet gallery) thumbnail; wheel / tap / button / keyboard zoom; drag-pan clamps correctly; prev/next + arrow keys cycle and reset zoom; `×` / backdrop / `Esc` all close and release the scroll lock and image src; single-image pieces open with no arrows and no counter; mobile 375px applies the tighter chrome media query with `document.documentElement.scrollWidth === clientWidth` (no horizontal overflow — the recurring mobile gotcha); no console errors through the whole pass.
