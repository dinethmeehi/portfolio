# ─── OS / editor cruft ────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.vscode/
.idea/

# ─── Claude Code local settings (machine-specific) ────────────────────
# launch.json + serve.ps1 are shared project config and ARE committed.
# settings.local.json is a per-machine permission allowlist — not shared.
.claude/settings.local.json

# ─── Working backups / scratch ───────────────────────────────────────
*.backup_*.html
portfolio_v2.backup_*
*.bak
*.bak_*
/scratch/
/tmp/

# ─── Files over GitHub's 100 MB per-file limit ───────────────────────
# Kept on disk, not tracked — a push containing them would be rejected.
#
# Raw 174 MB source export for the Ravager piece. The web version now
# streams from YouTube (id iZnEdqHjxqE); this original is local
# reference only.
/claude-md-project/RAVAGER/3D CHARACTER - RAVAGER.mp4
